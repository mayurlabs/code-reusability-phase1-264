/* ─── Interfaces ─── */

export interface ScanReport {
  id: string;
  name: string;
  requestedBy: string;
  requestedDate: string;
  startTime: string;
  endTime: string;
  environment: string;
  orgId: string;
  scope: string[];
  runtimeEnrichment: boolean;
  reportVersion: string;
  status: 'Complete' | 'In Progress' | 'Failed' | 'Draft';
  score: number | null;
  scoreDelta: number | null;
}

export interface DiffBlock {
  lineRange: string;
  representativeCode: string;
  copyCode: string;
  description: string;
}

export interface CloneCopy {
  id: string;
  fileName: string;
  className: string;
  methodName?: string;
  lineRange: string;
  lineCount: number;
  charCount: number;
  code: string[];
  referencedBy: string[];
  safeToRemove: boolean;
  safetyNote?: string;
  differences?: DiffBlock[];
}

export interface CriticalitySignal {
  icon: string;
  label: string;
  detail: string;
}

export interface CloneGroup {
  id: string;
  name: string;
  level: 'file' | 'method' | 'block';
  tier: 'HIGH' | 'MEDIUM' | 'LOW';
  category: 'business_logic' | 'data_access_logic' | 'file_duplicate' | 'orchestration' | 'simple_logic' | 'trivial_accessor';
  effort: 'LOW' | 'MEDIUM' | 'HIGH';
  copies: number;
  charsSaved: number;
  score: number;
  isExact: boolean;
  recipe: string;
  recipeDetail: string;
  representative: CloneCopy;
  cloneCopies: CloneCopy[];
  criticalitySignals: CriticalitySignal[];
  impact: {
    codeLines: string;
    workflows: string;
    changeRisk: string;
    testSurface: string;
    dependencies: string;
    governorLimits?: string;
  };
  whereItAppears: string[];
  whyItMatters: string[];
  nextSteps: string[];
  rationale: string[];
}

export interface AgentMessage {
  id: string;
  role: 'bot' | 'user';
  content: string;
  timestamp: string;
  actions?: { label: string; actionId: string }[];
}

/* ─── Scan Reports ─── */

export const scanReports: ScanReport[] = [
  { id: 'rpt-001', name: 'Mar 2026 Clone Detection Report - Northstar Retail Group', requestedBy: 'Priya Raman', requestedDate: '2026-03-26T09:12:00Z', startTime: '2026-03-26T09:12:00Z', endTime: '2026-03-26T09:19:00Z', environment: 'Production', orgId: '00D8A00000NRG42UAG', scope: ['Apex', 'Triggers', 'LWC JS', 'SOQL Patterns'], runtimeEnrichment: true, reportVersion: 'v1.4', status: 'Complete', score: 78, scoreDelta: 9 },
  { id: 'rpt-002', name: 'Mar 2026 Pre-Release Clone Review', requestedBy: 'Priya Raman', requestedDate: '2026-03-12T08:30:00Z', startTime: '2026-03-12T08:31:00Z', endTime: '2026-03-12T08:44:00Z', environment: 'Production', orgId: '00D8A00000NRG42UAG', scope: ['Apex', 'Triggers', 'LWC JS', 'SOQL Patterns'], runtimeEnrichment: true, reportVersion: 'v1.3', status: 'Complete', score: 69, scoreDelta: 4 },
  { id: 'rpt-003', name: 'Feb 2026 Monthly Clone Baseline', requestedBy: 'Priya Raman', requestedDate: '2026-02-20T10:00:00Z', startTime: '2026-02-20T10:01:00Z', endTime: '2026-02-20T10:12:00Z', environment: 'Production', orgId: '00D8A00000NRG42UAG', scope: ['Apex', 'Triggers', 'LWC JS'], runtimeEnrichment: true, reportVersion: 'v1.2', status: 'Complete', score: 65, scoreDelta: 3 },
  { id: 'rpt-004', name: 'Jan 2026 Initial Clone Baseline', requestedBy: 'Priya Raman', requestedDate: '2026-01-15T09:45:00Z', startTime: '2026-01-15T09:46:00Z', endTime: '2026-01-15T09:58:00Z', environment: 'Production', orgId: '00D8A00000NRG42UAG', scope: ['Apex', 'Triggers'], runtimeEnrichment: false, reportVersion: 'v1.0', status: 'Complete', score: 62, scoreDelta: null },
  { id: 'rpt-005', name: 'Mar 2026 Ad Hoc Sandbox Review', requestedBy: 'Daniel Brooks', requestedDate: '2026-03-18T14:15:00Z', startTime: '2026-03-18T14:16:00Z', endTime: '2026-03-18T14:19:00Z', environment: 'Full Copy Sandbox', orgId: '00D8A00000NRG42UAG', scope: ['Apex', 'Triggers', 'LWC JS'], runtimeEnrichment: false, reportVersion: 'v1.3', status: 'Failed', score: null, scoreDelta: null },
];

/* ─── Clone Groups ─── */

export const cloneGroups: CloneGroup[] = [

  /* ── Group 1: GetCasesByAssigneeIdDataView (EXACT, file, HIGH, file_duplicate) ── */
  {
    id: 'cg-001',
    name: 'GetCasesByAssigneeIdDataView.cls',
    level: 'file',
    tier: 'HIGH',
    category: 'file_duplicate',
    effort: 'LOW',
    copies: 7,
    charsSaved: 13608,
    score: 75,
    isExact: true,
    recipe: 'Delete duplicate file. Keep one copy, remove the other 6, and redirect callers.',
    recipeDetail: 'All 7 files extend BaseDataView and expose a REST endpoint that runs a parameterized SOQL query against Case. The only difference is the WHERE clause filter field. Consolidate into a single class that accepts the filter field as a parameter.',
    criticalitySignals: [
      { icon: '🔄', label: 'High Change Frequency', detail: 'Modified 11 times in the last 90 days across 4 of 7 copies' },
      { icon: '🐛', label: 'Bug Fix Drift', detail: 'A null-pointer fix was applied to GetCasesByAssigneeIdDataView but not to the other 6 copies' },
      { icon: '👥', label: 'Cross-Team Ownership', detail: 'Owned by 3 different teams with no shared review process' },
      { icon: '📡', label: 'Active Callers', detail: '15 controllers and services reference these copies across case management workflows' },
    ],
    representative: {
      id: 'cg-001-rep',
      fileName: 'GetCasesByAssigneeIdDataView.cls',
      className: 'GetCasesByAssigneeIdDataView',
      lineRange: '1-64',
      lineCount: 64,
      charCount: 2402,
      code: [
        '@RestResource(urlMapping=\'/cases/assignee/*\')',
        'global with sharing class GetCasesByAssigneeIdDataView extends BaseDataView {',
        '',
        '    @HttpGet',
        '    global static List<CaseDTO> getCases() {',
        '        RestRequest req = RestContext.request;',
        '        String assigneeId = req.requestURI.substringAfterLast(\'/\');',
        '',
        '        if (String.isBlank(assigneeId)) {',
        '            throw new DataViewException(\'Assignee ID is required\');',
        '        }',
        '',
        '        List<Case> cases = [',
        '            SELECT Id, CaseNumber, Subject, Status, Priority,',
        '                   CreatedDate, ClosedDate, Contact.Name,',
        '                   Account.Name, Owner.Name',
        '            FROM Case',
        '            WHERE OwnerId = :assigneeId',
        '            ORDER BY CreatedDate DESC',
        '            LIMIT 200',
        '        ];',
        '',
        '        List<CaseDTO> results = new List<CaseDTO>();',
        '        for (Case c : cases) {',
        '            CaseDTO dto = new CaseDTO();',
        '            dto.caseId = c.Id;',
        '            dto.caseNumber = c.CaseNumber;',
        '            dto.subject = c.Subject;',
        '            dto.status = c.Status;',
        '            dto.priority = c.Priority;',
        '            dto.contactName = c.Contact?.Name;',
        '            dto.accountName = c.Account?.Name;',
        '            dto.ownerName = c.Owner.Name;',
        '            dto.createdDate = c.CreatedDate;',
        '            dto.closedDate = c.ClosedDate;',
        '            results.add(dto);',
        '        }',
        '',
        '        logAccess(\'GetCasesByAssigneeIdDataView\', assigneeId);',
        '        return results;',
        '    }',
        '}',
      ],
      referencedBy: ['CaseAssignmentController', 'AgentWorkspaceService'],
      safeToRemove: false,
    },
    cloneCopies: [
      {
        id: 'cg-001-c1',
        fileName: 'GetCasesByCaseFolderIdDataView.cls',
        className: 'GetCasesByCaseFolderIdDataView',
        lineRange: '1-64',
        lineCount: 64,
        charCount: 2410,
        code: [
          '@RestResource(urlMapping=\'/cases/folder/*\')',
          'global with sharing class GetCasesByCaseFolderIdDataView extends BaseDataView {',
          '',
          '    @HttpGet',
          '    global static List<CaseDTO> getCases() {',
          '        RestRequest req = RestContext.request;',
          '        String folderId = req.requestURI.substringAfterLast(\'/\');',
          '',
          '        if (String.isBlank(folderId)) {',
          '            throw new DataViewException(\'Folder ID is required\');',
          '        }',
          '',
          '        List<Case> cases = [',
          '            SELECT Id, CaseNumber, Subject, Status, Priority,',
          '                   CreatedDate, ClosedDate, Contact.Name,',
          '                   Account.Name, Owner.Name',
          '            FROM Case',
          '            WHERE Case_Folder__c = :folderId',
          '            ORDER BY CreatedDate DESC',
          '            LIMIT 200',
          '        ];',
          '',
          '        List<CaseDTO> results = new List<CaseDTO>();',
          '        for (Case c : cases) {',
          '            CaseDTO dto = new CaseDTO();',
          '            dto.caseId = c.Id;',
          '            dto.caseNumber = c.CaseNumber;',
          '            dto.subject = c.Subject;',
          '            dto.status = c.Status;',
          '            dto.priority = c.Priority;',
          '            dto.contactName = c.Contact?.Name;',
          '            dto.accountName = c.Account?.Name;',
          '            dto.ownerName = c.Owner.Name;',
          '            dto.createdDate = c.CreatedDate;',
          '            dto.closedDate = c.ClosedDate;',
          '            results.add(dto);',
          '        }',
          '',
          '        logAccess(\'GetCasesByCaseFolderIdDataView\', folderId);',
          '        return results;',
          '    }',
          '}',
        ],
        referencedBy: ['CaseFolderController'],
        safeToRemove: true,
      },
      {
        id: 'cg-001-c2',
        fileName: 'GetCasesByCustomerDataView.cls',
        className: 'GetCasesByCustomerDataView',
        lineRange: '1-64',
        lineCount: 64,
        charCount: 2398,
        code: [
          '@RestResource(urlMapping=\'/cases/customer/*\')',
          'global with sharing class GetCasesByCustomerDataView extends BaseDataView {',
          '',
          '    @HttpGet',
          '    global static List<CaseDTO> getCases() {',
          '        RestRequest req = RestContext.request;',
          '        String customerId = req.requestURI.substringAfterLast(\'/\');',
          '',
          '        if (String.isBlank(customerId)) {',
          '            throw new DataViewException(\'Customer ID is required\');',
          '        }',
          '',
          '        List<Case> cases = [',
          '            SELECT Id, CaseNumber, Subject, Status, Priority,',
          '                   CreatedDate, ClosedDate, Contact.Name,',
          '                   Account.Name, Owner.Name',
          '            FROM Case',
          '            WHERE ContactId = :customerId',
          '            ORDER BY CreatedDate DESC',
          '            LIMIT 200',
          '        ];',
          '',
          '        List<CaseDTO> results = new List<CaseDTO>();',
          '        for (Case c : cases) {',
          '            CaseDTO dto = new CaseDTO();',
          '            dto.caseId = c.Id;',
          '            dto.caseNumber = c.CaseNumber;',
          '            dto.subject = c.Subject;',
          '            dto.status = c.Status;',
          '            dto.priority = c.Priority;',
          '            dto.contactName = c.Contact?.Name;',
          '            dto.accountName = c.Account?.Name;',
          '            dto.ownerName = c.Owner.Name;',
          '            dto.createdDate = c.CreatedDate;',
          '            dto.closedDate = c.ClosedDate;',
          '            results.add(dto);',
          '        }',
          '',
          '        logAccess(\'GetCasesByCustomerDataView\', customerId);',
          '        return results;',
          '    }',
          '}',
        ],
        referencedBy: ['CustomerPortalController', 'GuestSupportService'],
        safeToRemove: true,
      },
      {
        id: 'cg-001-c3',
        fileName: 'GetCasesByIdsDataView.cls',
        className: 'GetCasesByIdsDataView',
        lineRange: '1-64',
        lineCount: 64,
        charCount: 2390,
        code: [
          '@RestResource(urlMapping=\'/cases/ids/*\')',
          'global with sharing class GetCasesByIdsDataView extends BaseDataView {',
          '',
          '    @HttpGet',
          '    global static List<CaseDTO> getCases() {',
          '        RestRequest req = RestContext.request;',
          '        String idsParam = req.requestURI.substringAfterLast(\'/\');',
          '        List<String> caseIds = idsParam.split(\',\');',
          '',
          '        if (caseIds.isEmpty()) {',
          '            throw new DataViewException(\'At least one Case ID is required\');',
          '        }',
          '',
          '        List<Case> cases = [',
          '            SELECT Id, CaseNumber, Subject, Status, Priority,',
          '                   CreatedDate, ClosedDate, Contact.Name,',
          '                   Account.Name, Owner.Name',
          '            FROM Case',
          '            WHERE Id IN :caseIds',
          '            ORDER BY CreatedDate DESC',
          '            LIMIT 200',
          '        ];',
          '',
          '        List<CaseDTO> results = new List<CaseDTO>();',
          '        for (Case c : cases) {',
          '            CaseDTO dto = new CaseDTO();',
          '            dto.caseId = c.Id;',
          '            dto.caseNumber = c.CaseNumber;',
          '            dto.subject = c.Subject;',
          '            dto.status = c.Status;',
          '            dto.priority = c.Priority;',
          '            dto.contactName = c.Contact?.Name;',
          '            dto.accountName = c.Account?.Name;',
          '            dto.ownerName = c.Owner.Name;',
          '            dto.createdDate = c.CreatedDate;',
          '            dto.closedDate = c.ClosedDate;',
          '            results.add(dto);',
          '        }',
          '',
          '        logAccess(\'GetCasesByIdsDataView\', String.join(caseIds, \',\'));',
          '        return results;',
          '    }',
          '}',
        ],
        referencedBy: ['CaseBulkActionController'],
        safeToRemove: true,
      },
      {
        id: 'cg-001-c4',
        fileName: 'GetCasesByItemEntityTokenDataView.cls',
        className: 'GetCasesByItemEntityTokenDataView',
        lineRange: '1-64',
        lineCount: 64,
        charCount: 2418,
        code: [
          '@RestResource(urlMapping=\'/cases/entity-token/*\')',
          'global with sharing class GetCasesByItemEntityTokenDataView extends BaseDataView {',
          '',
          '    @HttpGet',
          '    global static List<CaseDTO> getCases() {',
          '        RestRequest req = RestContext.request;',
          '        String entityToken = req.requestURI.substringAfterLast(\'/\');',
          '',
          '        if (String.isBlank(entityToken)) {',
          '            throw new DataViewException(\'Entity Token is required\');',
          '        }',
          '',
          '        List<Case> cases = [',
          '            SELECT Id, CaseNumber, Subject, Status, Priority,',
          '                   CreatedDate, ClosedDate, Contact.Name,',
          '                   Account.Name, Owner.Name',
          '            FROM Case',
          '            WHERE Item_Entity_Token__c = :entityToken',
          '            ORDER BY CreatedDate DESC',
          '            LIMIT 200',
          '        ];',
          '',
          '        List<CaseDTO> results = new List<CaseDTO>();',
          '        for (Case c : cases) {',
          '            CaseDTO dto = new CaseDTO();',
          '            dto.caseId = c.Id;',
          '            dto.caseNumber = c.CaseNumber;',
          '            dto.subject = c.Subject;',
          '            dto.status = c.Status;',
          '            dto.priority = c.Priority;',
          '            dto.contactName = c.Contact?.Name;',
          '            dto.accountName = c.Account?.Name;',
          '            dto.ownerName = c.Owner.Name;',
          '            dto.createdDate = c.CreatedDate;',
          '            dto.closedDate = c.ClosedDate;',
          '            results.add(dto);',
          '        }',
          '',
          '        logAccess(\'GetCasesByItemEntityTokenDataView\', entityToken);',
          '        return results;',
          '    }',
          '}',
        ],
        referencedBy: ['ItemEntityCaseController'],
        safeToRemove: true,
      },
      {
        id: 'cg-001-c5',
        fileName: 'GetCasesByListingIdDataView.cls',
        className: 'GetCasesByListingIdDataView',
        lineRange: '1-64',
        lineCount: 64,
        charCount: 2395,
        code: [
          '@RestResource(urlMapping=\'/cases/listing/*\')',
          'global with sharing class GetCasesByListingIdDataView extends BaseDataView {',
          '',
          '    @HttpGet',
          '    global static List<CaseDTO> getCases() {',
          '        RestRequest req = RestContext.request;',
          '        String listingId = req.requestURI.substringAfterLast(\'/\');',
          '',
          '        if (String.isBlank(listingId)) {',
          '            throw new DataViewException(\'Listing ID is required\');',
          '        }',
          '',
          '        List<Case> cases = [',
          '            SELECT Id, CaseNumber, Subject, Status, Priority,',
          '                   CreatedDate, ClosedDate, Contact.Name,',
          '                   Account.Name, Owner.Name',
          '            FROM Case',
          '            WHERE Listing__c = :listingId',
          '            ORDER BY CreatedDate DESC',
          '            LIMIT 200',
          '        ];',
          '',
          '        List<CaseDTO> results = new List<CaseDTO>();',
          '        for (Case c : cases) {',
          '            CaseDTO dto = new CaseDTO();',
          '            dto.caseId = c.Id;',
          '            dto.caseNumber = c.CaseNumber;',
          '            dto.subject = c.Subject;',
          '            dto.status = c.Status;',
          '            dto.priority = c.Priority;',
          '            dto.contactName = c.Contact?.Name;',
          '            dto.accountName = c.Account?.Name;',
          '            dto.ownerName = c.Owner.Name;',
          '            dto.createdDate = c.CreatedDate;',
          '            dto.closedDate = c.ClosedDate;',
          '            results.add(dto);',
          '        }',
          '',
          '        logAccess(\'GetCasesByListingIdDataView\', listingId);',
          '        return results;',
          '    }',
          '}',
        ],
        referencedBy: ['ListingDetailController', 'ListingCaseWidget'],
        safeToRemove: true,
      },
      {
        id: 'cg-001-c6',
        fileName: 'GetCasesByReservationDataView.cls',
        className: 'GetCasesByReservationDataView',
        lineRange: '1-64',
        lineCount: 64,
        charCount: 2405,
        code: [
          '@RestResource(urlMapping=\'/cases/reservation/*\')',
          'global with sharing class GetCasesByReservationDataView extends BaseDataView {',
          '',
          '    @HttpGet',
          '    global static List<CaseDTO> getCases() {',
          '        RestRequest req = RestContext.request;',
          '        String reservationId = req.requestURI.substringAfterLast(\'/\');',
          '',
          '        if (String.isBlank(reservationId)) {',
          '            throw new DataViewException(\'Reservation ID is required\');',
          '        }',
          '',
          '        List<Case> cases = [',
          '            SELECT Id, CaseNumber, Subject, Status, Priority,',
          '                   CreatedDate, ClosedDate, Contact.Name,',
          '                   Account.Name, Owner.Name',
          '            FROM Case',
          '            WHERE Reservation__c = :reservationId',
          '            ORDER BY CreatedDate DESC',
          '            LIMIT 200',
          '        ];',
          '',
          '        List<CaseDTO> results = new List<CaseDTO>();',
          '        for (Case c : cases) {',
          '            CaseDTO dto = new CaseDTO();',
          '            dto.caseId = c.Id;',
          '            dto.caseNumber = c.CaseNumber;',
          '            dto.subject = c.Subject;',
          '            dto.status = c.Status;',
          '            dto.priority = c.Priority;',
          '            dto.contactName = c.Contact?.Name;',
          '            dto.accountName = c.Account?.Name;',
          '            dto.ownerName = c.Owner.Name;',
          '            dto.createdDate = c.CreatedDate;',
          '            dto.closedDate = c.ClosedDate;',
          '            results.add(dto);',
          '        }',
          '',
          '        logAccess(\'GetCasesByReservationDataView\', reservationId);',
          '        return results;',
          '    }',
          '}',
        ],
        referencedBy: ['ReservationDetailController'],
        safeToRemove: true,
      },
    ],
    impact: {
      codeLines: '448 total lines across 7 files; 384 removable',
      workflows: 'Case lookup REST endpoints used by 8 internal consumers',
      changeRisk: 'Low — all files are structurally identical',
      testSurface: '7 near-identical test classes can be consolidated into 1',
      dependencies: '8 inbound callers across case management controllers',
    },
    whereItAppears: ['Case Management', 'Guest Support Portal', 'Listing Management', 'Reservation Support'],
    whyItMatters: [
      'Seven identical files waste storage and create confusion about which version is canonical.',
      'A bug fix to the DTO mapping code must be applied 7 times.',
      'Each file has its own test class, multiplying test maintenance effort by 7x.',
    ],
    nextSteps: [
      'Create a single parameterized GetCasesByFilterDataView class that accepts a filter field and value.',
      'Redirect all 8 callers to the consolidated class.',
      'Delete the 6 duplicate files and their associated test classes.',
    ],
    rationale: [
      'All 7 files share identical DTO mapping, error handling, and query structure.',
      'The only variation is the WHERE clause filter field, which can be parameterized.',
    ],
  },

  /* ── Group 2: GuestPastClaimsSummaryGenerator (EXACT, file, HIGH, file_duplicate) ── */
  {
    id: 'cg-002',
    name: 'GuestPastClaimsSummaryGenerator.cls',
    level: 'file',
    tier: 'HIGH',
    category: 'file_duplicate',
    effort: 'LOW',
    copies: 3,
    charsSaved: 6654,
    score: 80,
    isExact: true,
    recipe: 'Delete duplicate file. These 3 files are identical.',
    recipeDetail: 'Three identical copies of the guest past-claims summary generator exist across different packages. They all query Claim__c records, aggregate totals, and produce a ClaimsSummaryDTO. Keep the version in the claims-core package and remove the other two.',
    criticalitySignals: [
      { icon: '🐛', label: 'Bug Fix Drift', detail: 'A currency formatting fix was applied to 1 copy but the other 2 still use the old format' },
      { icon: '🔄', label: 'Recent Changes', detail: 'Modified 4 times in the last 60 days' },
      { icon: '📋', label: 'Upcoming Release', detail: 'Claims summary code is scheduled for changes in the next sprint' },
    ],
    representative: {
      id: 'cg-002-rep',
      fileName: 'GuestPastClaimsSummaryGenerator.cls',
      className: 'GuestPastClaimsSummaryGenerator',
      lineRange: '1-58',
      lineCount: 58,
      charCount: 2218,
      code: [
        'public with sharing class GuestPastClaimsSummaryGenerator {',
        '',
        '    public static ClaimsSummaryDTO generateSummary(Id guestId) {',
        '        if (guestId == null) {',
        '            throw new ClaimsException(\'Guest ID cannot be null\');',
        '        }',
        '',
        '        List<Claim__c> pastClaims = [',
        '            SELECT Id, Claim_Number__c, Status__c, Amount__c,',
        '                   Currency__c, Filed_Date__c, Resolution_Date__c,',
        '                   Category__c, Payout_Amount__c',
        '            FROM Claim__c',
        '            WHERE Guest__c = :guestId',
        '              AND Status__c IN (\'Resolved\', \'Paid\', \'Denied\')',
        '            ORDER BY Filed_Date__c DESC',
        '        ];',
        '',
        '        ClaimsSummaryDTO summary = new ClaimsSummaryDTO();',
        '        summary.guestId = guestId;',
        '        summary.totalClaims = pastClaims.size();',
        '        summary.totalRequested = 0;',
        '        summary.totalPaid = 0;',
        '',
        '        for (Claim__c claim : pastClaims) {',
        '            summary.totalRequested += claim.Amount__c != null ? claim.Amount__c : 0;',
        '            summary.totalPaid += claim.Payout_Amount__c != null ? claim.Payout_Amount__c : 0;',
        '            if (claim.Status__c == \'Denied\') {',
        '                summary.deniedCount++;',
        '            }',
        '        }',
        '',
        '        summary.approvalRate = summary.totalClaims > 0',
        '            ? (summary.totalClaims - summary.deniedCount) * 100.0 / summary.totalClaims',
        '            : 0;',
        '',
        '        return summary;',
        '    }',
        '}',
      ],
      referencedBy: ['ClaimsDashboardController', 'GuestProfileService'],
      safeToRemove: false,
    },
    cloneCopies: [
      {
        id: 'cg-002-c1',
        fileName: 'GuestPastClaimsSummaryGenerator_v2.cls',
        className: 'GuestPastClaimsSummaryGenerator_v2',
        lineRange: '1-58',
        lineCount: 58,
        charCount: 2218,
        code: [
          'public with sharing class GuestPastClaimsSummaryGenerator_v2 {',
          '',
          '    public static ClaimsSummaryDTO generateSummary(Id guestId) {',
          '        if (guestId == null) {',
          '            throw new ClaimsException(\'Guest ID cannot be null\');',
          '        }',
          '',
          '        List<Claim__c> pastClaims = [',
          '            SELECT Id, Claim_Number__c, Status__c, Amount__c,',
          '                   Currency__c, Filed_Date__c, Resolution_Date__c,',
          '                   Category__c, Payout_Amount__c',
          '            FROM Claim__c',
          '            WHERE Guest__c = :guestId',
          '              AND Status__c IN (\'Resolved\', \'Paid\', \'Denied\')',
          '            ORDER BY Filed_Date__c DESC',
          '        ];',
          '',
          '        ClaimsSummaryDTO summary = new ClaimsSummaryDTO();',
          '        summary.guestId = guestId;',
          '        summary.totalClaims = pastClaims.size();',
          '        summary.totalRequested = 0;',
          '        summary.totalPaid = 0;',
          '',
          '        for (Claim__c claim : pastClaims) {',
          '            summary.totalRequested += claim.Amount__c != null ? claim.Amount__c : 0;',
          '            summary.totalPaid += claim.Payout_Amount__c != null ? claim.Payout_Amount__c : 0;',
          '            if (claim.Status__c == \'Denied\') {',
          '                summary.deniedCount++;',
          '            }',
          '        }',
          '',
          '        summary.approvalRate = summary.totalClaims > 0',
          '            ? (summary.totalClaims - summary.deniedCount) * 100.0 / summary.totalClaims',
          '            : 0;',
          '',
          '        return summary;',
          '    }',
          '}',
        ],
        referencedBy: ['LegacyClaimsController'],
        safeToRemove: true,
      },
      {
        id: 'cg-002-c2',
        fileName: 'GuestClaimsSummaryHelper.cls',
        className: 'GuestClaimsSummaryHelper',
        lineRange: '1-58',
        lineCount: 58,
        charCount: 2218,
        code: [
          'public with sharing class GuestClaimsSummaryHelper {',
          '',
          '    public static ClaimsSummaryDTO generateSummary(Id guestId) {',
          '        if (guestId == null) {',
          '            throw new ClaimsException(\'Guest ID cannot be null\');',
          '        }',
          '',
          '        List<Claim__c> pastClaims = [',
          '            SELECT Id, Claim_Number__c, Status__c, Amount__c,',
          '                   Currency__c, Filed_Date__c, Resolution_Date__c,',
          '                   Category__c, Payout_Amount__c',
          '            FROM Claim__c',
          '            WHERE Guest__c = :guestId',
          '              AND Status__c IN (\'Resolved\', \'Paid\', \'Denied\')',
          '            ORDER BY Filed_Date__c DESC',
          '        ];',
          '',
          '        ClaimsSummaryDTO summary = new ClaimsSummaryDTO();',
          '        summary.guestId = guestId;',
          '        summary.totalClaims = pastClaims.size();',
          '        summary.totalRequested = 0;',
          '        summary.totalPaid = 0;',
          '',
          '        for (Claim__c claim : pastClaims) {',
          '            summary.totalRequested += claim.Amount__c != null ? claim.Amount__c : 0;',
          '            summary.totalPaid += claim.Payout_Amount__c != null ? claim.Payout_Amount__c : 0;',
          '            if (claim.Status__c == \'Denied\') {',
          '                summary.deniedCount++;',
          '            }',
          '        }',
          '',
          '        summary.approvalRate = summary.totalClaims > 0',
          '            ? (summary.totalClaims - summary.deniedCount) * 100.0 / summary.totalClaims',
          '            : 0;',
          '',
          '        return summary;',
          '    }',
          '}',
        ],
        referencedBy: ['GuestSupportBatchJob'],
        safeToRemove: true,
      },
    ],
    impact: {
      codeLines: '174 total lines across 3 files; 116 removable',
      workflows: 'Claims summary generation used by dashboard and batch jobs',
      changeRisk: 'Low — byte-identical code',
      testSurface: '3 duplicate test classes',
      dependencies: '4 inbound callers',
    },
    whereItAppears: ['Claims Processing', 'Guest Support Portal', 'Claims Dashboard'],
    whyItMatters: [
      'Three identical files create ambiguity about which is the canonical version.',
      'Any claims calculation fix must be applied to all 3 copies.',
    ],
    nextSteps: [
      'Delete GuestPastClaimsSummaryGenerator_v2 and GuestClaimsSummaryHelper.',
      'Redirect LegacyClaimsController and GuestSupportBatchJob to use GuestPastClaimsSummaryGenerator.',
    ],
    rationale: [
      'All 3 files are byte-identical except for the class name.',
      'The original in claims-core has the most callers and highest test coverage.',
    ],
  },

  /* ── Group 3: ClaimPaymentCurrencyTriggerHandler (EXACT, file, HIGH, file_duplicate) ── */
  {
    id: 'cg-003',
    name: 'ClaimPaymentCurrencyTriggerHandler.cls',
    level: 'file',
    tier: 'HIGH',
    category: 'file_duplicate',
    effort: 'LOW',
    copies: 3,
    charsSaved: 6058,
    score: 82,
    isExact: true,
    recipe: 'Delete duplicate file.',
    recipeDetail: 'Three identical copies of the trigger handler for claim payment currency conversion. The handler converts claim amounts from local currency to the corporate currency using exchange rates. Keep the version registered in the trigger framework.',
    criticalitySignals: [
      { icon: '⚡', label: 'Governor Limit Risk', detail: '2 of 3 copies contain SOQL inside a trigger loop — risk of hitting query limits on bulk operations' },
      { icon: '🔄', label: 'Change Frequency', detail: 'Exchange rate code was updated 6 times in the last 90 days' },
      { icon: '🛡️', label: 'Compliance Impact', detail: 'Currency conversion accuracy affects financial reporting compliance' },
    ],
    representative: {
      id: 'cg-003-rep',
      fileName: 'ClaimPaymentCurrencyTriggerHandler.cls',
      className: 'ClaimPaymentCurrencyTriggerHandler',
      lineRange: '1-52',
      lineCount: 52,
      charCount: 2020,
      code: [
        'public with sharing class ClaimPaymentCurrencyTriggerHandler implements ITriggerHandler {',
        '',
        '    public void handle(TriggerContext ctx) {',
        '        if (ctx.isAfter && ctx.isInsert) {',
        '            convertPaymentCurrency(ctx.newList);',
        '        }',
        '    }',
        '',
        '    private void convertPaymentCurrency(List<Claim_Payment__c> payments) {',
        '        Set<String> currencyCodes = new Set<String>();',
        '        for (Claim_Payment__c pmt : payments) {',
        '            if (pmt.Currency_Code__c != null && pmt.Currency_Code__c != \'USD\') {',
        '                currencyCodes.add(pmt.Currency_Code__c);',
        '            }',
        '        }',
        '',
        '        if (currencyCodes.isEmpty()) { return; }',
        '',
        '        Map<String, Decimal> rates = new Map<String, Decimal>();',
        '        for (Exchange_Rate__c er : [',
        '            SELECT Currency_Code__c, Rate_To_USD__c',
        '            FROM Exchange_Rate__c',
        '            WHERE Currency_Code__c IN :currencyCodes',
        '              AND Effective_Date__c <= TODAY',
        '            ORDER BY Effective_Date__c DESC',
        '        ]) {',
        '            if (!rates.containsKey(er.Currency_Code__c)) {',
        '                rates.put(er.Currency_Code__c, er.Rate_To_USD__c);',
        '            }',
        '        }',
        '',
        '        List<Claim_Payment__c> toUpdate = new List<Claim_Payment__c>();',
        '        for (Claim_Payment__c pmt : payments) {',
        '            if (rates.containsKey(pmt.Currency_Code__c)) {',
        '                pmt.Converted_Amount_USD__c = pmt.Amount__c * rates.get(pmt.Currency_Code__c);',
        '                toUpdate.add(pmt);',
        '            }',
        '        }',
        '',
        '        if (!toUpdate.isEmpty()) {',
        '            update toUpdate;',
        '        }',
        '    }',
        '}',
      ],
      referencedBy: ['ClaimPaymentTrigger', 'TriggerDispatcher'],
      safeToRemove: false,
    },
    cloneCopies: [
      {
        id: 'cg-003-c1',
        fileName: 'ClaimPaymentCurrencyHandler_Legacy.cls',
        className: 'ClaimPaymentCurrencyHandler_Legacy',
        lineRange: '1-52',
        lineCount: 52,
        charCount: 2020,
        code: [
          'public with sharing class ClaimPaymentCurrencyHandler_Legacy implements ITriggerHandler {',
          '',
          '    public void handle(TriggerContext ctx) {',
          '        if (ctx.isAfter && ctx.isInsert) {',
          '            convertPaymentCurrency(ctx.newList);',
          '        }',
          '    }',
          '',
          '    private void convertPaymentCurrency(List<Claim_Payment__c> payments) {',
          '        Set<String> currencyCodes = new Set<String>();',
          '        for (Claim_Payment__c pmt : payments) {',
          '            if (pmt.Currency_Code__c != null && pmt.Currency_Code__c != \'USD\') {',
          '                currencyCodes.add(pmt.Currency_Code__c);',
          '            }',
          '        }',
          '',
          '        if (currencyCodes.isEmpty()) { return; }',
          '',
          '        Map<String, Decimal> rates = new Map<String, Decimal>();',
          '        for (Exchange_Rate__c er : [',
          '            SELECT Currency_Code__c, Rate_To_USD__c',
          '            FROM Exchange_Rate__c',
          '            WHERE Currency_Code__c IN :currencyCodes',
          '              AND Effective_Date__c <= TODAY',
          '            ORDER BY Effective_Date__c DESC',
          '        ]) {',
          '            if (!rates.containsKey(er.Currency_Code__c)) {',
          '                rates.put(er.Currency_Code__c, er.Rate_To_USD__c);',
          '            }',
          '        }',
          '',
          '        List<Claim_Payment__c> toUpdate = new List<Claim_Payment__c>();',
          '        for (Claim_Payment__c pmt : payments) {',
          '            if (rates.containsKey(pmt.Currency_Code__c)) {',
          '                pmt.Converted_Amount_USD__c = pmt.Amount__c * rates.get(pmt.Currency_Code__c);',
          '                toUpdate.add(pmt);',
          '            }',
          '        }',
          '',
          '        if (!toUpdate.isEmpty()) {',
          '            update toUpdate;',
          '        }',
          '    }',
          '}',
        ],
        referencedBy: ['LegacyClaimPaymentTrigger'],
        safeToRemove: true,
      },
      {
        id: 'cg-003-c2',
        fileName: 'PaymentCurrencyConversionHandler.cls',
        className: 'PaymentCurrencyConversionHandler',
        lineRange: '1-52',
        lineCount: 52,
        charCount: 2018,
        code: [
          'public with sharing class PaymentCurrencyConversionHandler implements ITriggerHandler {',
          '',
          '    public void handle(TriggerContext ctx) {',
          '        if (ctx.isAfter && ctx.isInsert) {',
          '            convertPaymentCurrency(ctx.newList);',
          '        }',
          '    }',
          '',
          '    private void convertPaymentCurrency(List<Claim_Payment__c> payments) {',
          '        Set<String> currencyCodes = new Set<String>();',
          '        for (Claim_Payment__c pmt : payments) {',
          '            if (pmt.Currency_Code__c != null && pmt.Currency_Code__c != \'USD\') {',
          '                currencyCodes.add(pmt.Currency_Code__c);',
          '            }',
          '        }',
          '',
          '        if (currencyCodes.isEmpty()) { return; }',
          '',
          '        Map<String, Decimal> rates = new Map<String, Decimal>();',
          '        for (Exchange_Rate__c er : [',
          '            SELECT Currency_Code__c, Rate_To_USD__c',
          '            FROM Exchange_Rate__c',
          '            WHERE Currency_Code__c IN :currencyCodes',
          '              AND Effective_Date__c <= TODAY',
          '            ORDER BY Effective_Date__c DESC',
          '        ]) {',
          '            if (!rates.containsKey(er.Currency_Code__c)) {',
          '                rates.put(er.Currency_Code__c, er.Rate_To_USD__c);',
          '            }',
          '        }',
          '',
          '        List<Claim_Payment__c> toUpdate = new List<Claim_Payment__c>();',
          '        for (Claim_Payment__c pmt : payments) {',
          '            if (rates.containsKey(pmt.Currency_Code__c)) {',
          '                pmt.Converted_Amount_USD__c = pmt.Amount__c * rates.get(pmt.Currency_Code__c);',
          '                toUpdate.add(pmt);',
          '            }',
          '        }',
          '',
          '        if (!toUpdate.isEmpty()) {',
          '            update toUpdate;',
          '        }',
          '    }',
          '}',
        ],
        referencedBy: ['SandboxClaimPaymentTrigger'],
        safeToRemove: true,
      },
    ],
    impact: {
      codeLines: '156 total lines across 3 files; 104 removable',
      workflows: 'Claim payment currency conversion on insert',
      changeRisk: 'Low — identical code, only class names differ',
      testSurface: '3 duplicate test classes',
      dependencies: '3 trigger registrations pointing to identical handlers',
    },
    whereItAppears: ['Claims Processing', 'Payment Management', 'Multi-Currency Support'],
    whyItMatters: [
      'Multiple trigger handlers for the same functionality risk double-execution if both triggers fire.',
      'Exchange rate code changes must be replicated across all 3 files.',
    ],
    nextSteps: [
      'Deregister the legacy and sandbox trigger handlers.',
      'Delete ClaimPaymentCurrencyHandler_Legacy and PaymentCurrencyConversionHandler.',
      'Verify only ClaimPaymentCurrencyTriggerHandler is active in production.',
    ],
    rationale: [
      'All 3 handlers are byte-identical except class name.',
      'Only one should be registered with the trigger dispatcher at a time.',
    ],
  },

  /* ── Group 4: SummaryAuditGenerator.fetchGuestClaim (NEAR DUPLICATE, method, HIGH, business_logic) ── */
  {
    id: 'cg-004',
    name: 'SummaryAuditGenerator.fetchGuestClaim...',
    level: 'method',
    tier: 'HIGH',
    category: 'business_logic',
    effort: 'MEDIUM',
    copies: 2,
    charsSaved: 5860,
    score: 70,
    isExact: false,
    recipe: 'Extract shared method. Both methods fetch guest claims with minor parameter differences.',
    recipeDetail: 'SummaryAuditGenerator and VlocityAuditGenerator both contain a fetchGuestClaimData method that queries Claim__c and builds an audit record. The representative uses explicit error handling with try-catch; the copy uses a simplified approach returning null on failure. Merge into one method with configurable error strategy.',
    criticalitySignals: [
      { icon: '🐛', label: 'Inconsistent Error Handling', detail: 'Representative throws exceptions; copy silently returns null — same callers get different failure behavior' },
      { icon: '🧪', label: 'Test Failure', detail: 'Copy has 2 failing unit tests related to null handling that representative passes' },
      { icon: '📡', label: 'Active Callers', detail: '8 services call these methods across audit and compliance workflows' },
    ],
    representative: {
      id: 'cg-004-rep',
      fileName: 'SummaryAuditGenerator.cls',
      className: 'SummaryAuditGenerator',
      methodName: 'fetchGuestClaimData',
      lineRange: '45-82',
      lineCount: 38,
      charCount: 1520,
      code: [
        'public static AuditRecord fetchGuestClaimData(Id guestId, Date startDate, Date endDate) {',
        '    try {',
        '        List<Claim__c> claims = [',
        '            SELECT Id, Claim_Number__c, Amount__c, Status__c,',
        '                   Category__c, Filed_Date__c, Resolution_Date__c',
        '            FROM Claim__c',
        '            WHERE Guest__c = :guestId',
        '              AND Filed_Date__c >= :startDate',
        '              AND Filed_Date__c <= :endDate',
        '            ORDER BY Filed_Date__c ASC',
        '        ];',
        '',
        '        AuditRecord audit = new AuditRecord();',
        '        audit.guestId = guestId;',
        '        audit.periodStart = startDate;',
        '        audit.periodEnd = endDate;',
        '        audit.claimCount = claims.size();',
        '        audit.totalAmount = 0;',
        '',
        '        for (Claim__c c : claims) {',
        '            audit.totalAmount += c.Amount__c != null ? c.Amount__c : 0;',
        '        }',
        '',
        '        audit.generatedDate = Datetime.now();',
        '        audit.status = \'Complete\';',
        '        return audit;',
        '    } catch (Exception e) {',
        '        AuditErrorLogger.log(\'fetchGuestClaimData\', e);',
        '        throw new AuditException(\'Failed to fetch guest claim data: \' + e.getMessage());',
        '    }',
        '}',
      ],
      referencedBy: ['ClaimsAuditBatchJob', 'AuditReportController'],
      safeToRemove: false,
    },
    cloneCopies: [
      {
        id: 'cg-004-c1',
        fileName: 'VlocityAuditGenerator.cls',
        className: 'VlocityAuditGenerator',
        methodName: 'fetchGuestClaimData',
        lineRange: '38-70',
        lineCount: 33,
        charCount: 1340,
        code: [
          'public static AuditRecord fetchGuestClaimData(Id guestId, Date fromDate, Date toDate) {',
          '    List<Claim__c> claims = [',
          '        SELECT Id, Claim_Number__c, Amount__c, Status__c,',
          '               Category__c, Filed_Date__c, Resolution_Date__c',
          '        FROM Claim__c',
          '        WHERE Guest__c = :guestId',
          '          AND Filed_Date__c >= :fromDate',
          '          AND Filed_Date__c <= :toDate',
          '        ORDER BY Filed_Date__c ASC',
          '    ];',
          '',
          '    if (claims.isEmpty()) {',
          '        return null;',
          '    }',
          '',
          '    AuditRecord audit = new AuditRecord();',
          '    audit.guestId = guestId;',
          '    audit.periodStart = fromDate;',
          '    audit.periodEnd = toDate;',
          '    audit.claimCount = claims.size();',
          '    audit.totalAmount = 0;',
          '',
          '    for (Claim__c c : claims) {',
          '        audit.totalAmount += c.Amount__c != null ? c.Amount__c : 0;',
          '    }',
          '',
          '    audit.generatedDate = Datetime.now();',
          '    audit.status = \'Complete\';',
          '    return audit;',
          '}',
        ],
        referencedBy: ['VlocityAuditBatchJob'],
        safeToRemove: false,
        safetyNote: 'Callers depend on null-return behavior for empty claims; must update callers if merged.',
        differences: [
          {
            lineRange: '1',
            representativeCode: 'public static AuditRecord fetchGuestClaimData(Id guestId, Date startDate, Date endDate) {',
            copyCode: 'public static AuditRecord fetchGuestClaimData(Id guestId, Date fromDate, Date toDate) {',
            description: 'Parameter names differ: startDate/endDate vs fromDate/toDate',
          },
          {
            lineRange: '12-14',
            representativeCode: '    // (no early return — proceeds to build audit with claimCount=0)',
            copyCode: '    if (claims.isEmpty()) {\n        return null;\n    }',
            description: 'Copy returns null for empty claims; representative always returns an AuditRecord',
          },
          {
            lineRange: '27-30',
            representativeCode: '    } catch (Exception e) {\n        AuditErrorLogger.log(\'fetchGuestClaimData\', e);\n        throw new AuditException(\'Failed to fetch guest claim data: \' + e.getMessage());\n    }',
            copyCode: '    // (no try-catch — exceptions propagate to caller)',
            description: 'Representative wraps code in try-catch with error logging; copy lets exceptions propagate',
          },
        ],
      },
    ],
    impact: {
      codeLines: '71 lines across 2 methods; ~30 lines of shared code',
      workflows: 'Audit report generation for guest claims',
      changeRisk: 'Medium — different error handling must be reconciled',
      testSurface: '2 test classes with overlapping test assertions',
      dependencies: '3 inbound callers across batch jobs and controllers',
    },
    whereItAppears: ['Claims Processing', 'Audit Reporting', 'Guest Support Portal'],
    whyItMatters: [
      'Near-identical claim fetching code diverges in error handling, creating inconsistent behavior.',
      'A SOQL field change in Claim__c must be applied to both methods.',
    ],
    nextSteps: [
      'Extract a shared fetchGuestClaimData method with a configurable error strategy parameter.',
      'Update VlocityAuditBatchJob to handle the new error behavior.',
      'Consolidate test coverage into a single parameterized test class.',
    ],
    rationale: [
      'Core query and aggregation code is identical; only error handling and parameter names differ.',
      'The representative version is more robust with explicit error logging.',
    ],
  },

  /* ── Group 5: VlocitySummaryAuditGenerator.fetchHostSummary (NEAR DUPLICATE, method, HIGH, data_access_logic) ── */
  {
    id: 'cg-005',
    name: 'VlocitySummaryAuditGenerator.fetchHos...',
    level: 'method',
    tier: 'HIGH',
    category: 'data_access_logic',
    effort: 'MEDIUM',
    copies: 2,
    charsSaved: 5270,
    score: 68,
    isExact: false,
    recipe: 'Extract shared method. Consolidate host summary fetching code into a single shared method.',
    recipeDetail: 'Two methods fetch host summary data from Reservation__c with slightly different field selections and date filtering. The representative queries by host ID with a check-in date filter; the copy queries by listing ID with a created-date filter. The aggregation and DTO mapping code is identical.',
    criticalitySignals: [
      { icon: '🔄', label: 'Change Frequency', detail: 'Host summary queries modified 5 times in the last 90 days' },
      { icon: '📊', label: 'Data Inconsistency', detail: 'Different field selections produce different summary results for the same host' },
      { icon: '👥', label: 'Cross-Team Ownership', detail: 'Owned by 2 teams — neither has visibility into the other copy' },
    ],
    representative: {
      id: 'cg-005-rep',
      fileName: 'VlocitySummaryAuditGenerator.cls',
      className: 'VlocitySummaryAuditGenerator',
      methodName: 'fetchHostSummary',
      lineRange: '88-120',
      lineCount: 33,
      charCount: 1380,
      code: [
        'public static HostSummaryDTO fetchHostSummary(Id hostId, Date periodStart, Date periodEnd) {',
        '    List<Reservation__c> reservations = [',
        '        SELECT Id, Confirmation_Code__c, Check_In_Date__c,',
        '               Check_Out_Date__c, Total_Payout__c, Status__c,',
        '               Guest__r.Name, Listing__r.Name',
        '        FROM Reservation__c',
        '        WHERE Host__c = :hostId',
        '          AND Check_In_Date__c >= :periodStart',
        '          AND Check_In_Date__c <= :periodEnd',
        '          AND Status__c != \'Cancelled\'',
        '        ORDER BY Check_In_Date__c DESC',
        '    ];',
        '',
        '    HostSummaryDTO summary = new HostSummaryDTO();',
        '    summary.hostId = hostId;',
        '    summary.reservationCount = reservations.size();',
        '    summary.totalPayout = 0;',
        '    summary.averageStayLength = 0;',
        '',
        '    for (Reservation__c r : reservations) {',
        '        summary.totalPayout += r.Total_Payout__c != null ? r.Total_Payout__c : 0;',
        '        Integer nights = r.Check_In_Date__c.daysBetween(r.Check_Out_Date__c);',
        '        summary.averageStayLength += nights;',
        '    }',
        '',
        '    if (summary.reservationCount > 0) {',
        '        summary.averageStayLength = summary.averageStayLength / summary.reservationCount;',
        '    }',
        '',
        '    return summary;',
        '}',
      ],
      referencedBy: ['HostDashboardController', 'HostPayoutBatchJob'],
      safeToRemove: false,
    },
    cloneCopies: [
      {
        id: 'cg-005-c1',
        fileName: 'ListingPerformanceGenerator.cls',
        className: 'ListingPerformanceGenerator',
        methodName: 'fetchListingSummary',
        lineRange: '52-86',
        lineCount: 35,
        charCount: 1420,
        code: [
          'public static HostSummaryDTO fetchListingSummary(Id listingId, Date fromDate, Date toDate) {',
          '    List<Reservation__c> reservations = [',
          '        SELECT Id, Confirmation_Code__c, Check_In_Date__c,',
          '               Check_Out_Date__c, Total_Payout__c, Status__c,',
          '               Guest__r.Name, Listing__r.Name, Nightly_Rate__c',
          '        FROM Reservation__c',
          '        WHERE Listing__c = :listingId',
          '          AND CreatedDate >= :fromDate',
          '          AND CreatedDate <= :toDate',
          '          AND Status__c != \'Cancelled\'',
          '        ORDER BY CreatedDate DESC',
          '    ];',
          '',
          '    HostSummaryDTO summary = new HostSummaryDTO();',
          '    summary.hostId = null;',
          '    summary.reservationCount = reservations.size();',
          '    summary.totalPayout = 0;',
          '    summary.averageStayLength = 0;',
          '',
          '    for (Reservation__c r : reservations) {',
          '        summary.totalPayout += r.Total_Payout__c != null ? r.Total_Payout__c : 0;',
          '        Integer nights = r.Check_In_Date__c.daysBetween(r.Check_Out_Date__c);',
          '        summary.averageStayLength += nights;',
          '    }',
          '',
          '    if (summary.reservationCount > 0) {',
          '        summary.averageStayLength = summary.averageStayLength / summary.reservationCount;',
          '    }',
          '',
          '    return summary;',
          '}',
        ],
        referencedBy: ['ListingAnalyticsController', 'PerformanceReportBatch'],
        safeToRemove: false,
        safetyNote: 'Queries by Listing__c and CreatedDate instead of Host__c and Check_In_Date__c; callers expect listing-scoped results.',
        differences: [
          {
            lineRange: '1',
            representativeCode: 'public static HostSummaryDTO fetchHostSummary(Id hostId, Date periodStart, Date periodEnd) {',
            copyCode: 'public static HostSummaryDTO fetchListingSummary(Id listingId, Date fromDate, Date toDate) {',
            description: 'Method name and parameter differ: hostId vs listingId',
          },
          {
            lineRange: '5',
            representativeCode: '               Guest__r.Name, Listing__r.Name',
            copyCode: '               Guest__r.Name, Listing__r.Name, Nightly_Rate__c',
            description: 'Copy selects an additional field: Nightly_Rate__c',
          },
          {
            lineRange: '7-9',
            representativeCode: '        WHERE Host__c = :hostId\n          AND Check_In_Date__c >= :periodStart\n          AND Check_In_Date__c <= :periodEnd',
            copyCode: '        WHERE Listing__c = :listingId\n          AND CreatedDate >= :fromDate\n          AND CreatedDate <= :toDate',
            description: 'Filter field and date column differ: Host__c/Check_In_Date__c vs Listing__c/CreatedDate',
          },
        ],
      },
    ],
    impact: {
      codeLines: '68 lines across 2 methods; ~50 lines of shared code',
      workflows: 'Host and listing performance reporting',
      changeRisk: 'Medium — different filter semantics must be parameterized',
      testSurface: '2 test classes with similar reservation factory methods',
      dependencies: '4 inbound callers',
    },
    whereItAppears: ['Host Dashboard', 'Listing Analytics', 'Payout Processing'],
    whyItMatters: [
      'Identical aggregation code maintained in two places risks drift when payout calculation rules change.',
      'The HostSummaryDTO is reused but mapped differently, creating subtle bugs.',
    ],
    nextSteps: [
      'Create a shared fetchReservationSummary method that accepts filter field, filter value, and date field as parameters.',
      'Update both callers to use the consolidated method.',
    ],
    rationale: [
      'Aggregation code (payout sum, average stay) is identical.',
      'Only the SOQL WHERE clause and one SELECT field differ.',
    ],
  },

  /* ── Group 6: FeedCommentTriggerHandler (EXACT, file, HIGH, file_duplicate) ── */
  {
    id: 'cg-006',
    name: 'FeedCommentTriggerHandler.cls',
    level: 'file',
    tier: 'HIGH',
    category: 'file_duplicate',
    effort: 'LOW',
    copies: 2,
    charsSaved: 4597,
    score: 85,
    isExact: true,
    recipe: 'Delete duplicate file.',
    recipeDetail: 'Two identical copies of the FeedComment trigger handler. Both filter profanity, validate mention permissions, and post moderation audit records. The copy was created during a sandbox refresh and never cleaned up.',
    criticalitySignals: [
      { icon: '📋', label: 'Sandbox Artifact', detail: 'Copy was created during a sandbox refresh 8 months ago and never cleaned up' },
      { icon: '🔄', label: 'Recent Changes', detail: 'Profanity filter rules were updated in 1 copy but not the other' },
    ],
    representative: {
      id: 'cg-006-rep',
      fileName: 'FeedCommentTriggerHandler.cls',
      className: 'FeedCommentTriggerHandler',
      lineRange: '1-48',
      lineCount: 48,
      charCount: 2299,
      code: [
        'public with sharing class FeedCommentTriggerHandler implements ITriggerHandler {',
        '',
        '    private static final Set<String> BLOCKED_WORDS = ProfanityFilter.getBlockedWords();',
        '',
        '    public void handle(TriggerContext ctx) {',
        '        if (ctx.isBefore && ctx.isInsert) {',
        '            validateComments(ctx.newList);',
        '        }',
        '        if (ctx.isAfter && ctx.isInsert) {',
        '            createModerationAudit(ctx.newList);',
        '        }',
        '    }',
        '',
        '    private void validateComments(List<FeedComment> comments) {',
        '        for (FeedComment fc : comments) {',
        '            String body = fc.CommentBody != null ? fc.CommentBody.toLowerCase() : \'\';',
        '            for (String word : BLOCKED_WORDS) {',
        '                if (body.contains(word)) {',
        '                    fc.addError(\'Comment contains prohibited content.\');',
        '                    break;',
        '                }',
        '            }',
        '        }',
        '    }',
        '',
        '    private void createModerationAudit(List<FeedComment> comments) {',
        '        List<Moderation_Audit__c> audits = new List<Moderation_Audit__c>();',
        '        for (FeedComment fc : comments) {',
        '            audits.add(new Moderation_Audit__c(',
        '                Feed_Comment_Id__c = fc.Id,',
        '                Author_Id__c = fc.CreatedById,',
        '                Comment_Length__c = fc.CommentBody != null ? fc.CommentBody.length() : 0,',
        '                Created_Date__c = Datetime.now()',
        '            ));',
        '        }',
        '        if (!audits.isEmpty()) {',
        '            insert audits;',
        '        }',
        '    }',
        '}',
      ],
      referencedBy: ['FeedCommentTrigger', 'TriggerDispatcher'],
      safeToRemove: false,
    },
    cloneCopies: [
      {
        id: 'cg-006-c1',
        fileName: 'FeedCommentTriggerHandler_Backup.cls',
        className: 'FeedCommentTriggerHandler_Backup',
        lineRange: '1-48',
        lineCount: 48,
        charCount: 2298,
        code: [
          'public with sharing class FeedCommentTriggerHandler_Backup implements ITriggerHandler {',
          '',
          '    private static final Set<String> BLOCKED_WORDS = ProfanityFilter.getBlockedWords();',
          '',
          '    public void handle(TriggerContext ctx) {',
          '        if (ctx.isBefore && ctx.isInsert) {',
          '            validateComments(ctx.newList);',
          '        }',
          '        if (ctx.isAfter && ctx.isInsert) {',
          '            createModerationAudit(ctx.newList);',
          '        }',
          '    }',
          '',
          '    private void validateComments(List<FeedComment> comments) {',
          '        for (FeedComment fc : comments) {',
          '            String body = fc.CommentBody != null ? fc.CommentBody.toLowerCase() : \'\';',
          '            for (String word : BLOCKED_WORDS) {',
          '                if (body.contains(word)) {',
          '                    fc.addError(\'Comment contains prohibited content.\');',
          '                    break;',
          '                }',
          '            }',
          '        }',
          '    }',
          '',
          '    private void createModerationAudit(List<FeedComment> comments) {',
          '        List<Moderation_Audit__c> audits = new List<Moderation_Audit__c>();',
          '        for (FeedComment fc : comments) {',
          '            audits.add(new Moderation_Audit__c(',
          '                Feed_Comment_Id__c = fc.Id,',
          '                Author_Id__c = fc.CreatedById,',
          '                Comment_Length__c = fc.CommentBody != null ? fc.CommentBody.length() : 0,',
          '                Created_Date__c = Datetime.now()',
          '            ));',
          '        }',
          '        if (!audits.isEmpty()) {',
          '            insert audits;',
          '        }',
          '    }',
          '}',
        ],
        referencedBy: ['(none — orphaned)'],
        safeToRemove: true,
      },
    ],
    impact: {
      codeLines: '96 total lines across 2 files; 48 removable',
      workflows: 'Feed comment moderation and audit logging',
      changeRisk: 'Low — identical code, backup copy is orphaned',
      testSurface: '2 test classes (backup test class is never run)',
      dependencies: 'Backup has 0 active callers',
    },
    whereItAppears: ['Chatter Moderation', 'Community Feed', 'Compliance Auditing'],
    whyItMatters: [
      'The backup copy is orphaned and wastes org metadata space.',
      'Developers may accidentally reference the backup instead of the active handler.',
    ],
    nextSteps: [
      'Delete FeedCommentTriggerHandler_Backup and its test class.',
      'Verify no deployment scripts reference the backup class.',
    ],
    rationale: [
      'The backup copy has zero inbound references — it is completely orphaned.',
      'Identical code means the backup provides no additional value.',
    ],
  },

  /* ── Group 7: GuestReviewSummaryGenerator (EXACT, file, HIGH, file_duplicate) ── */
  {
    id: 'cg-007',
    name: 'GuestReviewSummaryGenerator.cls',
    level: 'file',
    tier: 'HIGH',
    category: 'file_duplicate',
    effort: 'LOW',
    copies: 2,
    charsSaved: 3354,
    score: 88,
    isExact: true,
    recipe: 'Delete duplicate file.',
    recipeDetail: 'Two identical copies of the guest review summary generator. Both aggregate review scores and produce a ReviewSummaryDTO. The copy was introduced during a package restructuring.',
    criticalitySignals: [
      { icon: '📋', label: 'Package Restructure Artifact', detail: 'Copy was introduced during a package restructuring and is no longer needed' },
      { icon: '🧪', label: 'Duplicate Tests', detail: '2 identical test classes exist — 140 lines of duplicate test code' },
    ],
    representative: {
      id: 'cg-007-rep',
      fileName: 'GuestReviewSummaryGenerator.cls',
      className: 'GuestReviewSummaryGenerator',
      lineRange: '1-42',
      lineCount: 42,
      charCount: 1677,
      code: [
        'public with sharing class GuestReviewSummaryGenerator {',
        '',
        '    public static ReviewSummaryDTO generateSummary(Id guestId) {',
        '        List<Review__c> reviews = [',
        '            SELECT Id, Rating__c, Review_Text__c, Created_Date__c,',
        '                   Listing__r.Name, Host__r.Name',
        '            FROM Review__c',
        '            WHERE Guest__c = :guestId',
        '              AND Status__c = \'Published\'',
        '            ORDER BY Created_Date__c DESC',
        '        ];',
        '',
        '        ReviewSummaryDTO summary = new ReviewSummaryDTO();',
        '        summary.guestId = guestId;',
        '        summary.totalReviews = reviews.size();',
        '        summary.averageRating = 0;',
        '',
        '        Decimal totalRating = 0;',
        '        for (Review__c r : reviews) {',
        '            totalRating += r.Rating__c != null ? r.Rating__c : 0;',
        '        }',
        '',
        '        if (summary.totalReviews > 0) {',
        '            summary.averageRating = totalRating / summary.totalReviews;',
        '            summary.averageRating = summary.averageRating.setScale(1, RoundingMode.HALF_UP);',
        '        }',
        '',
        '        return summary;',
        '    }',
        '}',
      ],
      referencedBy: ['GuestProfileController', 'ReviewAggregationBatch'],
      safeToRemove: false,
    },
    cloneCopies: [
      {
        id: 'cg-007-c1',
        fileName: 'GuestReviewSummaryHelper.cls',
        className: 'GuestReviewSummaryHelper',
        lineRange: '1-42',
        lineCount: 42,
        charCount: 1677,
        code: [
          'public with sharing class GuestReviewSummaryHelper {',
          '',
          '    public static ReviewSummaryDTO generateSummary(Id guestId) {',
          '        List<Review__c> reviews = [',
          '            SELECT Id, Rating__c, Review_Text__c, Created_Date__c,',
          '                   Listing__r.Name, Host__r.Name',
          '            FROM Review__c',
          '            WHERE Guest__c = :guestId',
          '              AND Status__c = \'Published\'',
          '            ORDER BY Created_Date__c DESC',
          '        ];',
          '',
          '        ReviewSummaryDTO summary = new ReviewSummaryDTO();',
          '        summary.guestId = guestId;',
          '        summary.totalReviews = reviews.size();',
          '        summary.averageRating = 0;',
          '',
          '        Decimal totalRating = 0;',
          '        for (Review__c r : reviews) {',
          '            totalRating += r.Rating__c != null ? r.Rating__c : 0;',
          '        }',
          '',
          '        if (summary.totalReviews > 0) {',
          '            summary.averageRating = totalRating / summary.totalReviews;',
          '            summary.averageRating = summary.averageRating.setScale(1, RoundingMode.HALF_UP);',
          '        }',
          '',
          '        return summary;',
          '    }',
          '}',
        ],
        referencedBy: ['LegacyGuestPortalController'],
        safeToRemove: true,
      },
    ],
    impact: {
      codeLines: '84 total lines across 2 files; 42 removable',
      workflows: 'Guest review aggregation for profiles and dashboards',
      changeRisk: 'Low — identical code',
      testSurface: '2 duplicate test classes',
      dependencies: '3 inbound callers',
    },
    whereItAppears: ['Guest Profiles', 'Review Management', 'Host Dashboard'],
    whyItMatters: [
      'Duplicate review summary code risks inconsistent rating calculations if one copy is updated.',
    ],
    nextSteps: [
      'Delete GuestReviewSummaryHelper.',
      'Update LegacyGuestPortalController to reference GuestReviewSummaryGenerator.',
    ],
    rationale: [
      'Both files are byte-identical except for the class name.',
      'GuestReviewSummaryGenerator has more callers and is in the canonical package.',
    ],
  },

  /* ── Group 8: ViaductListingDataService.buildListingDTO (NEAR DUPLICATE, method, HIGH, business_logic) ── */
  {
    id: 'cg-008',
    name: 'ViaductListingDataService.buildListin...',
    level: 'method',
    tier: 'HIGH',
    category: 'business_logic',
    effort: 'MEDIUM',
    copies: 2,
    charsSaved: 3225,
    score: 65,
    isExact: false,
    recipe: 'Extract shared method. Listing data construction code is nearly identical across both methods.',
    recipeDetail: 'Two methods construct a ListingDTO from a Listing__c record. The representative maps all standard fields including amenities; the copy omits amenities but adds a pricing breakdown. The core field mapping (name, location, host, status) is identical.',
    criticalitySignals: [
      { icon: '📊', label: 'Data Inconsistency', detail: 'One copy includes amenities, the other adds pricing — callers get different data for the same listing' },
      { icon: '📡', label: 'Active Callers', detail: '6 LWC components reference these methods for listing display' },
      { icon: '📋', label: 'Upcoming Release', detail: 'Listing data model changes are planned for next quarter — both copies will need updating' },
    ],
    representative: {
      id: 'cg-008-rep',
      fileName: 'ViaductListingDataService.cls',
      className: 'ViaductListingDataService',
      methodName: 'buildListingDTO',
      lineRange: '112-140',
      lineCount: 29,
      charCount: 1150,
      code: [
        'private static ListingDTO buildListingDTO(Listing__c listing) {',
        '    ListingDTO dto = new ListingDTO();',
        '    dto.listingId = listing.Id;',
        '    dto.name = listing.Name;',
        '    dto.propertyType = listing.Property_Type__c;',
        '    dto.city = listing.City__c;',
        '    dto.state = listing.State__c;',
        '    dto.country = listing.Country__c;',
        '    dto.hostId = listing.Host__c;',
        '    dto.hostName = listing.Host__r.Name;',
        '    dto.status = listing.Status__c;',
        '    dto.nightlyRate = listing.Nightly_Rate__c;',
        '    dto.maxGuests = listing.Max_Guests__c != null ? listing.Max_Guests__c.intValue() : 0;',
        '    dto.bedrooms = listing.Bedrooms__c != null ? listing.Bedrooms__c.intValue() : 0;',
        '    dto.bathrooms = listing.Bathrooms__c != null ? listing.Bathrooms__c.intValue() : 0;',
        '',
        '    List<String> amenities = new List<String>();',
        '    for (Listing_Amenity__c la : listing.Listing_Amenities__r) {',
        '        amenities.add(la.Amenity__r.Name);',
        '    }',
        '    dto.amenities = amenities;',
        '',
        '    return dto;',
        '}',
      ],
      referencedBy: ['ViaductListingDataService.getListingDetail', 'ViaductListingDataService.getListingsByHost'],
      safeToRemove: false,
    },
    cloneCopies: [
      {
        id: 'cg-008-c1',
        fileName: 'SearchListingMapper.cls',
        className: 'SearchListingMapper',
        methodName: 'mapToListingDTO',
        lineRange: '24-55',
        lineCount: 32,
        charCount: 1275,
        code: [
          'private static ListingDTO mapToListingDTO(Listing__c listing) {',
          '    ListingDTO dto = new ListingDTO();',
          '    dto.listingId = listing.Id;',
          '    dto.name = listing.Name;',
          '    dto.propertyType = listing.Property_Type__c;',
          '    dto.city = listing.City__c;',
          '    dto.state = listing.State__c;',
          '    dto.country = listing.Country__c;',
          '    dto.hostId = listing.Host__c;',
          '    dto.hostName = listing.Host__r.Name;',
          '    dto.status = listing.Status__c;',
          '    dto.nightlyRate = listing.Nightly_Rate__c;',
          '    dto.maxGuests = listing.Max_Guests__c != null ? listing.Max_Guests__c.intValue() : 0;',
          '    dto.bedrooms = listing.Bedrooms__c != null ? listing.Bedrooms__c.intValue() : 0;',
          '    dto.bathrooms = listing.Bathrooms__c != null ? listing.Bathrooms__c.intValue() : 0;',
          '',
          '    dto.cleaningFee = listing.Cleaning_Fee__c;',
          '    dto.serviceFee = listing.Service_Fee__c;',
          '    dto.totalPrice = (listing.Nightly_Rate__c != null ? listing.Nightly_Rate__c : 0)',
          '        + (listing.Cleaning_Fee__c != null ? listing.Cleaning_Fee__c : 0)',
          '        + (listing.Service_Fee__c != null ? listing.Service_Fee__c : 0);',
          '',
          '    return dto;',
          '}',
        ],
        referencedBy: ['SearchResultsController', 'SearchAPIHandler'],
        safeToRemove: false,
        safetyNote: 'Copy adds pricing breakdown fields (cleaningFee, serviceFee, totalPrice) not present in representative; callers depend on these.',
        differences: [
          {
            lineRange: '1',
            representativeCode: 'private static ListingDTO buildListingDTO(Listing__c listing) {',
            copyCode: 'private static ListingDTO mapToListingDTO(Listing__c listing) {',
            description: 'Method name differs: buildListingDTO vs mapToListingDTO',
          },
          {
            lineRange: '17-21',
            representativeCode: '    List<String> amenities = new List<String>();\n    for (Listing_Amenity__c la : listing.Listing_Amenities__r) {\n        amenities.add(la.Amenity__r.Name);\n    }\n    dto.amenities = amenities;',
            copyCode: '    dto.cleaningFee = listing.Cleaning_Fee__c;\n    dto.serviceFee = listing.Service_Fee__c;\n    dto.totalPrice = ...;',
            description: 'Representative maps amenities; copy maps pricing breakdown instead',
          },
        ],
      },
    ],
    impact: {
      codeLines: '61 lines across 2 methods; ~42 lines of shared mapping',
      workflows: 'Listing detail and search result display',
      changeRisk: 'Medium — shared DTO but different field sets',
      testSurface: '2 test classes with overlapping listing factory methods',
      dependencies: '4 inbound callers',
    },
    whereItAppears: ['Listing Management', 'Search Results', 'Host Dashboard'],
    whyItMatters: [
      'Core listing field mapping is duplicated — a field rename requires changes in both places.',
      'Different method names for the same pattern creates unnecessary cognitive load.',
    ],
    nextSteps: [
      'Extract a shared base mapping method for the common 14 fields.',
      'Add amenity and pricing enrichment as optional post-mapping steps.',
    ],
    rationale: [
      '14 of 16 field mappings are identical between the two methods.',
      'The different enrichments (amenities vs pricing) can be added as composable steps.',
    ],
  },

  /* ── Group 9: FSCEvidenceValidationController.completeValidation (NEAR DUPLICATE, method, HIGH, orchestration) ── */
  {
    id: 'cg-009',
    name: 'FSCEvidenceValidationController.compl...',
    level: 'method',
    tier: 'HIGH',
    category: 'orchestration',
    effort: 'MEDIUM',
    copies: 2,
    charsSaved: 3201,
    score: 62,
    isExact: false,
    recipe: 'Extract shared method. Validation orchestration can be parameterized.',
    recipeDetail: 'Two methods orchestrate evidence validation workflows: fetching evidence records, running validation rules, recording results, and updating the parent case status. The representative validates financial services evidence; the copy validates insurance evidence. The orchestration flow is identical but the validation rules differ.',
    criticalitySignals: [
      { icon: '🛡️', label: 'Compliance Impact', detail: 'Evidence validation code affects regulatory compliance reporting' },
      { icon: '🐛', label: 'Rule Drift', detail: 'Financial validation rules were updated but insurance validation rules were not — same orchestration, different rule outcomes' },
      { icon: '📡', label: 'Active Callers', detail: '4 case management workflows depend on these validation methods' },
    ],
    representative: {
      id: 'cg-009-rep',
      fileName: 'FSCEvidenceValidationController.cls',
      className: 'FSCEvidenceValidationController',
      methodName: 'completeValidation',
      lineRange: '34-68',
      lineCount: 35,
      charCount: 1420,
      code: [
        '@AuraEnabled',
        'public static ValidationResult completeValidation(Id caseId) {',
        '    Case parentCase = [SELECT Id, Status, Type, Sub_Type__c FROM Case WHERE Id = :caseId];',
        '',
        '    List<Evidence__c> evidenceRecords = [',
        '        SELECT Id, Type__c, File_Name__c, Upload_Date__c, Verified__c',
        '        FROM Evidence__c',
        '        WHERE Case__c = :caseId',
        '          AND Verified__c = false',
        '    ];',
        '',
        '    ValidationResult result = new ValidationResult();',
        '    result.caseId = caseId;',
        '    result.totalEvidence = evidenceRecords.size();',
        '    result.passedCount = 0;',
        '    result.failedCount = 0;',
        '',
        '    for (Evidence__c ev : evidenceRecords) {',
        '        Boolean passed = FSCValidationRules.validate(ev, parentCase.Type);',
        '        ev.Verified__c = passed;',
        '        if (passed) { result.passedCount++; }',
        '        else { result.failedCount++; }',
        '    }',
        '',
        '    update evidenceRecords;',
        '',
        '    if (result.failedCount == 0) {',
        '        parentCase.Status = \'Evidence Verified\';',
        '    } else {',
        '        parentCase.Status = \'Evidence Review Required\';',
        '    }',
        '    update parentCase;',
        '',
        '    result.newCaseStatus = parentCase.Status;',
        '    return result;',
        '}',
      ],
      referencedBy: ['EvidenceValidationLWC', 'CaseWorkflowController'],
      safeToRemove: false,
    },
    cloneCopies: [
      {
        id: 'cg-009-c1',
        fileName: 'InsuranceEvidenceController.cls',
        className: 'InsuranceEvidenceController',
        methodName: 'runEvidenceValidation',
        lineRange: '28-64',
        lineCount: 37,
        charCount: 1481,
        code: [
          '@AuraEnabled',
          'public static ValidationResult runEvidenceValidation(Id caseId) {',
          '    Case parentCase = [SELECT Id, Status, Type, Policy_Type__c FROM Case WHERE Id = :caseId];',
          '',
          '    List<Evidence__c> evidenceRecords = [',
          '        SELECT Id, Type__c, File_Name__c, Upload_Date__c, Verified__c',
          '        FROM Evidence__c',
          '        WHERE Case__c = :caseId',
          '          AND Verified__c = false',
          '    ];',
          '',
          '    ValidationResult result = new ValidationResult();',
          '    result.caseId = caseId;',
          '    result.totalEvidence = evidenceRecords.size();',
          '    result.passedCount = 0;',
          '    result.failedCount = 0;',
          '',
          '    for (Evidence__c ev : evidenceRecords) {',
          '        Boolean passed = InsuranceValidationRules.validate(ev, parentCase.Policy_Type__c);',
          '        ev.Verified__c = passed;',
          '        if (passed) { result.passedCount++; }',
          '        else { result.failedCount++; }',
          '    }',
          '',
          '    update evidenceRecords;',
          '',
          '    if (result.failedCount == 0) {',
          '        parentCase.Status = \'Evidence Verified\';',
          '    } else {',
          '        parentCase.Status = \'Evidence Review Required\';',
          '    }',
          '    update parentCase;',
          '',
          '    result.newCaseStatus = parentCase.Status;',
          '    return result;',
          '}',
        ],
        referencedBy: ['InsuranceEvidenceLWC'],
        safeToRemove: false,
        safetyNote: 'Uses InsuranceValidationRules instead of FSCValidationRules; callers expect insurance-specific validation.',
        differences: [
          {
            lineRange: '2',
            representativeCode: 'public static ValidationResult completeValidation(Id caseId) {',
            copyCode: 'public static ValidationResult runEvidenceValidation(Id caseId) {',
            description: 'Method name differs: completeValidation vs runEvidenceValidation',
          },
          {
            lineRange: '3',
            representativeCode: '    Case parentCase = [SELECT Id, Status, Type, Sub_Type__c FROM Case WHERE Id = :caseId];',
            copyCode: '    Case parentCase = [SELECT Id, Status, Type, Policy_Type__c FROM Case WHERE Id = :caseId];',
            description: 'Case query selects different field: Sub_Type__c vs Policy_Type__c',
          },
          {
            lineRange: '19',
            representativeCode: '        Boolean passed = FSCValidationRules.validate(ev, parentCase.Type);',
            copyCode: '        Boolean passed = InsuranceValidationRules.validate(ev, parentCase.Policy_Type__c);',
            description: 'Different validation rules class and parameter: FSCValidationRules/Type vs InsuranceValidationRules/Policy_Type__c',
          },
        ],
      },
    ],
    impact: {
      codeLines: '72 lines across 2 methods; ~60 lines of shared orchestration',
      workflows: 'Evidence validation for FSC and Insurance case types',
      changeRisk: 'Medium — status transition code must remain consistent',
      testSurface: '2 test classes with overlapping evidence factory data',
      dependencies: '3 LWC components and controllers',
    },
    whereItAppears: ['Financial Services Case Management', 'Insurance Claims', 'Evidence Review Workflow'],
    whyItMatters: [
      'The validation orchestration (fetch, validate, update, set status) is duplicated with only the validation strategy differing.',
      'Status transition code changes must be applied in both places.',
    ],
    nextSteps: [
      'Extract a generic validateEvidence method that accepts a validation strategy interface.',
      'Implement FSCValidationStrategy and InsuranceValidationStrategy.',
      'Update both LWC controllers to call the consolidated method.',
    ],
    rationale: [
      'The orchestration pattern is identical; only the validation rules and the Case field used for context differ.',
      'A strategy pattern cleanly separates the variation points.',
    ],
  },

  /* ── Group 10: ViaductListingDataService.getListingData (NEAR DUPLICATE, method, MEDIUM, data_access_logic) ── */
  {
    id: 'cg-010',
    name: 'ViaductListingDataService.getListingD...',
    level: 'method',
    tier: 'MEDIUM',
    category: 'data_access_logic',
    effort: 'MEDIUM',
    copies: 6,
    charsSaved: 12660,
    score: 58,
    isExact: false,
    recipe: 'Extract and parameterize. 6 methods fetch listing data with different filters — consolidate into one configurable method.',
    recipeDetail: 'Six methods in different services query Listing__c with the same SELECT fields but different WHERE clauses. Each method filters by a different field (city, host, property type, status, price range, or availability dates). The DTO mapping and error handling are identical.',
    criticalitySignals: [
      { icon: '🔄', label: 'High Change Frequency', detail: 'Listing queries modified 14 times in the last 90 days across 4 of 6 copies' },
      { icon: '⚡', label: 'Governor Limit Risk', detail: '3 copies use non-selective SOQL filters that could hit query limits at scale' },
      { icon: '👥', label: 'Cross-Team Ownership', detail: 'Spread across 4 teams — changes in one copy are not propagated to others' },
      { icon: '📡', label: 'High Caller Count', detail: '22 components reference these methods across search, browse, and booking workflows' },
    ],
    representative: {
      id: 'cg-010-rep',
      fileName: 'ViaductListingDataService.cls',
      className: 'ViaductListingDataService',
      methodName: 'getListingDataByCity',
      lineRange: '15-42',
      lineCount: 28,
      charCount: 1120,
      code: [
        'public static List<ListingDTO> getListingDataByCity(String city) {',
        '    if (String.isBlank(city)) {',
        '        throw new ListingServiceException(\'City is required\');',
        '    }',
        '',
        '    List<Listing__c> listings = [',
        '        SELECT Id, Name, Property_Type__c, City__c, State__c, Country__c,',
        '               Host__c, Host__r.Name, Status__c, Nightly_Rate__c,',
        '               Max_Guests__c, Bedrooms__c, Bathrooms__c',
        '        FROM Listing__c',
        '        WHERE City__c = :city AND Status__c = \'Active\'',
        '        ORDER BY Nightly_Rate__c ASC',
        '        LIMIT 100',
        '    ];',
        '',
        '    List<ListingDTO> results = new List<ListingDTO>();',
        '    for (Listing__c l : listings) {',
        '        results.add(buildListingDTO(l));',
        '    }',
        '    return results;',
        '}',
      ],
      referencedBy: ['SearchController', 'CityBrowseLWC'],
      safeToRemove: false,
    },
    cloneCopies: [
      {
        id: 'cg-010-c1',
        fileName: 'ViaductListingDataService.cls',
        className: 'ViaductListingDataService',
        methodName: 'getListingDataByHost',
        lineRange: '44-71',
        lineCount: 28,
        charCount: 1115,
        code: [
          'public static List<ListingDTO> getListingDataByHost(Id hostId) {',
          '    if (hostId == null) {',
          '        throw new ListingServiceException(\'Host ID is required\');',
          '    }',
          '',
          '    List<Listing__c> listings = [',
          '        SELECT Id, Name, Property_Type__c, City__c, State__c, Country__c,',
          '               Host__c, Host__r.Name, Status__c, Nightly_Rate__c,',
          '               Max_Guests__c, Bedrooms__c, Bathrooms__c',
          '        FROM Listing__c',
          '        WHERE Host__c = :hostId',
          '        ORDER BY Name ASC',
          '        LIMIT 100',
          '    ];',
          '',
          '    List<ListingDTO> results = new List<ListingDTO>();',
          '    for (Listing__c l : listings) {',
          '        results.add(buildListingDTO(l));',
          '    }',
          '    return results;',
          '}',
        ],
        referencedBy: ['HostDashboardController'],
        safeToRemove: false,
        safetyNote: 'Filters by Host__c instead of City__c; does not filter by Status. Callers expect all statuses returned.',
        differences: [
          {
            lineRange: '1',
            representativeCode: 'public static List<ListingDTO> getListingDataByCity(String city) {',
            copyCode: 'public static List<ListingDTO> getListingDataByHost(Id hostId) {',
            description: 'Different method name and parameter type',
          },
          {
            lineRange: '11',
            representativeCode: '        WHERE City__c = :city AND Status__c = \'Active\'',
            copyCode: '        WHERE Host__c = :hostId',
            description: 'Different filter field and no status filter',
          },
          {
            lineRange: '12',
            representativeCode: '        ORDER BY Nightly_Rate__c ASC',
            copyCode: '        ORDER BY Name ASC',
            description: 'Different sort order',
          },
        ],
      },
      {
        id: 'cg-010-c2',
        fileName: 'ListingSearchService.cls',
        className: 'ListingSearchService',
        methodName: 'getListingsByPropertyType',
        lineRange: '10-37',
        lineCount: 28,
        charCount: 1125,
        code: [
          'public static List<ListingDTO> getListingsByPropertyType(String propertyType) {',
          '    if (String.isBlank(propertyType)) {',
          '        throw new ListingServiceException(\'Property type is required\');',
          '    }',
          '',
          '    List<Listing__c> listings = [',
          '        SELECT Id, Name, Property_Type__c, City__c, State__c, Country__c,',
          '               Host__c, Host__r.Name, Status__c, Nightly_Rate__c,',
          '               Max_Guests__c, Bedrooms__c, Bathrooms__c',
          '        FROM Listing__c',
          '        WHERE Property_Type__c = :propertyType AND Status__c = \'Active\'',
          '        ORDER BY Nightly_Rate__c ASC',
          '        LIMIT 100',
          '    ];',
          '',
          '    List<ListingDTO> results = new List<ListingDTO>();',
          '    for (Listing__c l : listings) {',
          '        results.add(buildListingDTO(l));',
          '    }',
          '    return results;',
          '}',
        ],
        referencedBy: ['PropertyTypeBrowseLWC'],
        safeToRemove: false,
        safetyNote: 'Filters by Property_Type__c; must verify callers before consolidation.',
        differences: [
          {
            lineRange: '1',
            representativeCode: 'public static List<ListingDTO> getListingDataByCity(String city) {',
            copyCode: 'public static List<ListingDTO> getListingsByPropertyType(String propertyType) {',
            description: 'Method name and parameter differ',
          },
          {
            lineRange: '11',
            representativeCode: '        WHERE City__c = :city AND Status__c = \'Active\'',
            copyCode: '        WHERE Property_Type__c = :propertyType AND Status__c = \'Active\'',
            description: 'Filter field differs: City__c vs Property_Type__c',
          },
        ],
      },
      {
        id: 'cg-010-c3',
        fileName: 'ListingSearchService.cls',
        className: 'ListingSearchService',
        methodName: 'getListingsByStatus',
        lineRange: '39-66',
        lineCount: 28,
        charCount: 1100,
        code: [
          'public static List<ListingDTO> getListingsByStatus(String status) {',
          '    if (String.isBlank(status)) {',
          '        throw new ListingServiceException(\'Status is required\');',
          '    }',
          '',
          '    List<Listing__c> listings = [',
          '        SELECT Id, Name, Property_Type__c, City__c, State__c, Country__c,',
          '               Host__c, Host__r.Name, Status__c, Nightly_Rate__c,',
          '               Max_Guests__c, Bedrooms__c, Bathrooms__c',
          '        FROM Listing__c',
          '        WHERE Status__c = :status',
          '        ORDER BY Name ASC',
          '        LIMIT 100',
          '    ];',
          '',
          '    List<ListingDTO> results = new List<ListingDTO>();',
          '    for (Listing__c l : listings) {',
          '        results.add(buildListingDTO(l));',
          '    }',
          '    return results;',
          '}',
        ],
        referencedBy: ['AdminListingDashboard'],
        safeToRemove: false,
        safetyNote: 'Filters by Status__c only; callers expect all statuses including inactive.',
        differences: [
          {
            lineRange: '1',
            representativeCode: 'public static List<ListingDTO> getListingDataByCity(String city) {',
            copyCode: 'public static List<ListingDTO> getListingsByStatus(String status) {',
            description: 'Method name and parameter differ',
          },
          {
            lineRange: '11',
            representativeCode: '        WHERE City__c = :city AND Status__c = \'Active\'',
            copyCode: '        WHERE Status__c = :status',
            description: 'Filter field differs and no compound filter',
          },
        ],
      },
      {
        id: 'cg-010-c4',
        fileName: 'ListingPricingService.cls',
        className: 'ListingPricingService',
        methodName: 'getListingsByPriceRange',
        lineRange: '12-42',
        lineCount: 31,
        charCount: 1180,
        code: [
          'public static List<ListingDTO> getListingsByPriceRange(Decimal minPrice, Decimal maxPrice) {',
          '    if (minPrice == null || maxPrice == null) {',
          '        throw new ListingServiceException(\'Price range is required\');',
          '    }',
          '',
          '    List<Listing__c> listings = [',
          '        SELECT Id, Name, Property_Type__c, City__c, State__c, Country__c,',
          '               Host__c, Host__r.Name, Status__c, Nightly_Rate__c,',
          '               Max_Guests__c, Bedrooms__c, Bathrooms__c',
          '        FROM Listing__c',
          '        WHERE Nightly_Rate__c >= :minPrice',
          '          AND Nightly_Rate__c <= :maxPrice',
          '          AND Status__c = \'Active\'',
          '        ORDER BY Nightly_Rate__c ASC',
          '        LIMIT 100',
          '    ];',
          '',
          '    List<ListingDTO> results = new List<ListingDTO>();',
          '    for (Listing__c l : listings) {',
          '        results.add(buildListingDTO(l));',
          '    }',
          '    return results;',
          '}',
        ],
        referencedBy: ['PriceFilterLWC', 'SearchAPIHandler'],
        safeToRemove: false,
        safetyNote: 'Uses range filter on Nightly_Rate__c; consolidation must preserve range semantics.',
        differences: [
          {
            lineRange: '1',
            representativeCode: 'public static List<ListingDTO> getListingDataByCity(String city) {',
            copyCode: 'public static List<ListingDTO> getListingsByPriceRange(Decimal minPrice, Decimal maxPrice) {',
            description: 'Method accepts two Decimal parameters for range filtering',
          },
          {
            lineRange: '11-13',
            representativeCode: '        WHERE City__c = :city AND Status__c = \'Active\'',
            copyCode: '        WHERE Nightly_Rate__c >= :minPrice\n          AND Nightly_Rate__c <= :maxPrice\n          AND Status__c = \'Active\'',
            description: 'Range filter on Nightly_Rate__c instead of equality on City__c',
          },
        ],
      },
      {
        id: 'cg-010-c5',
        fileName: 'AvailabilitySearchService.cls',
        className: 'AvailabilitySearchService',
        methodName: 'getAvailableListings',
        lineRange: '18-48',
        lineCount: 31,
        charCount: 1220,
        code: [
          'public static List<ListingDTO> getAvailableListings(Date checkIn, Date checkOut) {',
          '    if (checkIn == null || checkOut == null) {',
          '        throw new ListingServiceException(\'Check-in and check-out dates are required\');',
          '    }',
          '',
          '    Set<Id> bookedListingIds = new Set<Id>();',
          '    for (Reservation__c r : [',
          '        SELECT Listing__c FROM Reservation__c',
          '        WHERE Check_In_Date__c < :checkOut AND Check_Out_Date__c > :checkIn',
          '          AND Status__c != \'Cancelled\'',
          '    ]) {',
          '        bookedListingIds.add(r.Listing__c);',
          '    }',
          '',
          '    List<Listing__c> listings = [',
          '        SELECT Id, Name, Property_Type__c, City__c, State__c, Country__c,',
          '               Host__c, Host__r.Name, Status__c, Nightly_Rate__c,',
          '               Max_Guests__c, Bedrooms__c, Bathrooms__c',
          '        FROM Listing__c',
          '        WHERE Id NOT IN :bookedListingIds AND Status__c = \'Active\'',
          '        ORDER BY Nightly_Rate__c ASC',
          '        LIMIT 100',
          '    ];',
          '',
          '    List<ListingDTO> results = new List<ListingDTO>();',
          '    for (Listing__c l : listings) {',
          '        results.add(buildListingDTO(l));',
          '    }',
          '    return results;',
          '}',
        ],
        referencedBy: ['SearchController', 'AvailabilityCalendarLWC'],
        safeToRemove: false,
        safetyNote: 'Contains additional reservation-exclusion query; consolidation must preserve availability behavior.',
        differences: [
          {
            lineRange: '1',
            representativeCode: 'public static List<ListingDTO> getListingDataByCity(String city) {',
            copyCode: 'public static List<ListingDTO> getAvailableListings(Date checkIn, Date checkOut) {',
            description: 'Method accepts Date parameters and pre-filters booked listings',
          },
          {
            lineRange: '6-13',
            representativeCode: '    // (no pre-filter query)',
            copyCode: '    Set<Id> bookedListingIds = new Set<Id>();\n    for (Reservation__c r : [...]) {\n        bookedListingIds.add(r.Listing__c);\n    }',
            description: 'Copy includes an additional SOQL query to exclude booked listings',
          },
          {
            lineRange: '20',
            representativeCode: '        WHERE City__c = :city AND Status__c = \'Active\'',
            copyCode: '        WHERE Id NOT IN :bookedListingIds AND Status__c = \'Active\'',
            description: 'Filter uses NOT IN exclusion set instead of field equality',
          },
        ],
      },
    ],
    impact: {
      codeLines: '174 total lines across 6 methods; ~120 lines of shared query/mapping',
      workflows: 'Listing search, browse, filter, and availability checking',
      changeRisk: 'Medium — 6 methods must stay consistent for search UX',
      testSurface: '6 test methods with identical listing factory data',
      dependencies: '8 inbound callers across LWC and API handlers',
      governorLimits: 'Each method issues its own SOQL query; a combined approach could reduce query count',
    },
    whereItAppears: ['Search Results', 'City Browse', 'Host Dashboard', 'Price Filter', 'Availability Calendar', 'Admin Dashboard'],
    whyItMatters: [
      'Six methods share identical SELECT fields and DTO mapping but use different WHERE clauses.',
      'Adding a new field to Listing__c requires updating 6 SOQL queries.',
      'The availability method issues 2 SOQL queries — consolidation could optimize this.',
    ],
    nextSteps: [
      'Create a configurable getListings(ListingQueryConfig config) method.',
      'Move filter criteria into a ListingQueryConfig builder pattern.',
      'Migrate callers one at a time, starting with the simplest (city, status).',
    ],
    rationale: [
      'All 6 methods share identical SELECT, DTO mapping, and error handling.',
      'Only the WHERE clause varies, which is a prime candidate for parameterization.',
    ],
  },

  /* ── Group 11: ViaductReviewsService.buildGuestReview (NEAR DUPLICATE, method, MEDIUM, business_logic) ── */
  {
    id: 'cg-011',
    name: 'ViaductReviewsService.buildGuestRevie...',
    level: 'method',
    tier: 'MEDIUM',
    category: 'business_logic',
    effort: 'MEDIUM',
    copies: 3,
    charsSaved: 8260,
    score: 55,
    isExact: false,
    recipe: 'Extract and parameterize. Review construction code differs only in guest type.',
    recipeDetail: 'Three methods build review DTOs for different guest types (standard guest, business traveler, VIP guest). The core review mapping is identical; the variations add type-specific enrichment fields.',
    criticalitySignals: [
      { icon: '📊', label: 'Data Inconsistency', detail: 'VIP guest reviews include loyalty tier data that standard and business copies miss' },
      { icon: '🔄', label: 'Recent Changes', detail: 'Review scoring algorithm was updated in 1 of 3 copies last month' },
      { icon: '📡', label: 'Active Callers', detail: '5 review display components use these methods' },
    ],
    representative: {
      id: 'cg-011-rep',
      fileName: 'ViaductReviewsService.cls',
      className: 'ViaductReviewsService',
      methodName: 'buildGuestReviewDTO',
      lineRange: '22-48',
      lineCount: 27,
      charCount: 1080,
      code: [
        'private static ReviewDTO buildGuestReviewDTO(Review__c review) {',
        '    ReviewDTO dto = new ReviewDTO();',
        '    dto.reviewId = review.Id;',
        '    dto.guestId = review.Guest__c;',
        '    dto.guestName = review.Guest__r.Name;',
        '    dto.listingId = review.Listing__c;',
        '    dto.listingName = review.Listing__r.Name;',
        '    dto.rating = review.Rating__c;',
        '    dto.reviewText = review.Review_Text__c;',
        '    dto.createdDate = review.Created_Date__c;',
        '    dto.status = review.Status__c;',
        '    dto.hostResponse = review.Host_Response__c;',
        '',
        '    dto.categories = new Map<String, Decimal>();',
        '    dto.categories.put(\'Cleanliness\', review.Cleanliness_Rating__c);',
        '    dto.categories.put(\'Communication\', review.Communication_Rating__c);',
        '    dto.categories.put(\'Accuracy\', review.Accuracy_Rating__c);',
        '    dto.categories.put(\'Location\', review.Location_Rating__c);',
        '    dto.categories.put(\'Value\', review.Value_Rating__c);',
        '',
        '    return dto;',
        '}',
      ],
      referencedBy: ['ViaductReviewsService.getReviewsForListing'],
      safeToRemove: false,
    },
    cloneCopies: [
      {
        id: 'cg-011-c1',
        fileName: 'BusinessTravelReviewService.cls',
        className: 'BusinessTravelReviewService',
        methodName: 'buildBusinessReviewDTO',
        lineRange: '15-45',
        lineCount: 31,
        charCount: 1240,
        code: [
          'private static ReviewDTO buildBusinessReviewDTO(Review__c review) {',
          '    ReviewDTO dto = new ReviewDTO();',
          '    dto.reviewId = review.Id;',
          '    dto.guestId = review.Guest__c;',
          '    dto.guestName = review.Guest__r.Name;',
          '    dto.listingId = review.Listing__c;',
          '    dto.listingName = review.Listing__r.Name;',
          '    dto.rating = review.Rating__c;',
          '    dto.reviewText = review.Review_Text__c;',
          '    dto.createdDate = review.Created_Date__c;',
          '    dto.status = review.Status__c;',
          '    dto.hostResponse = review.Host_Response__c;',
          '',
          '    dto.categories = new Map<String, Decimal>();',
          '    dto.categories.put(\'Cleanliness\', review.Cleanliness_Rating__c);',
          '    dto.categories.put(\'Communication\', review.Communication_Rating__c);',
          '    dto.categories.put(\'Accuracy\', review.Accuracy_Rating__c);',
          '    dto.categories.put(\'Location\', review.Location_Rating__c);',
          '    dto.categories.put(\'Value\', review.Value_Rating__c);',
          '',
          '    dto.businessWifiRating = review.Wifi_Rating__c;',
          '    dto.businessWorkspaceRating = review.Workspace_Rating__c;',
          '    dto.companyName = review.Guest__r.Company_Name__c;',
          '',
          '    return dto;',
          '}',
        ],
        referencedBy: ['BusinessTravelPortalController'],
        safeToRemove: false,
        safetyNote: 'Adds business-specific fields (wifiRating, workspaceRating, companyName); callers depend on these.',
        differences: [
          {
            lineRange: '1',
            representativeCode: 'private static ReviewDTO buildGuestReviewDTO(Review__c review) {',
            copyCode: 'private static ReviewDTO buildBusinessReviewDTO(Review__c review) {',
            description: 'Method name differs',
          },
          {
            lineRange: '21-23',
            representativeCode: '    // (no business-specific fields)',
            copyCode: '    dto.businessWifiRating = review.Wifi_Rating__c;\n    dto.businessWorkspaceRating = review.Workspace_Rating__c;\n    dto.companyName = review.Guest__r.Company_Name__c;',
            description: 'Copy adds 3 business-travel-specific fields after the common mapping',
          },
        ],
      },
      {
        id: 'cg-011-c2',
        fileName: 'VIPReviewService.cls',
        className: 'VIPReviewService',
        methodName: 'buildVIPReviewDTO',
        lineRange: '18-46',
        lineCount: 29,
        charCount: 1160,
        code: [
          'private static ReviewDTO buildVIPReviewDTO(Review__c review) {',
          '    ReviewDTO dto = new ReviewDTO();',
          '    dto.reviewId = review.Id;',
          '    dto.guestId = review.Guest__c;',
          '    dto.guestName = review.Guest__r.Name;',
          '    dto.listingId = review.Listing__c;',
          '    dto.listingName = review.Listing__r.Name;',
          '    dto.rating = review.Rating__c;',
          '    dto.reviewText = review.Review_Text__c;',
          '    dto.createdDate = review.Created_Date__c;',
          '    dto.status = review.Status__c;',
          '    dto.hostResponse = review.Host_Response__c;',
          '',
          '    dto.categories = new Map<String, Decimal>();',
          '    dto.categories.put(\'Cleanliness\', review.Cleanliness_Rating__c);',
          '    dto.categories.put(\'Communication\', review.Communication_Rating__c);',
          '    dto.categories.put(\'Accuracy\', review.Accuracy_Rating__c);',
          '    dto.categories.put(\'Location\', review.Location_Rating__c);',
          '    dto.categories.put(\'Value\', review.Value_Rating__c);',
          '',
          '    dto.vipTier = review.Guest__r.VIP_Tier__c;',
          '    dto.conciergeAssigned = review.Concierge_Assigned__c;',
          '',
          '    return dto;',
          '}',
        ],
        referencedBy: ['VIPGuestDashboardController'],
        safeToRemove: false,
        safetyNote: 'Adds VIP-specific fields (vipTier, conciergeAssigned); must preserve for VIP dashboard.',
        differences: [
          {
            lineRange: '1',
            representativeCode: 'private static ReviewDTO buildGuestReviewDTO(Review__c review) {',
            copyCode: 'private static ReviewDTO buildVIPReviewDTO(Review__c review) {',
            description: 'Method name differs',
          },
          {
            lineRange: '21-22',
            representativeCode: '    // (no VIP-specific fields)',
            copyCode: '    dto.vipTier = review.Guest__r.VIP_Tier__c;\n    dto.conciergeAssigned = review.Concierge_Assigned__c;',
            description: 'Copy adds 2 VIP-specific fields after the common mapping',
          },
        ],
      },
    ],
    impact: {
      codeLines: '87 lines across 3 methods; ~60 lines of shared mapping',
      workflows: 'Review display across standard, business, and VIP guest portals',
      changeRisk: 'Medium — adding a new rating category requires updating all 3',
      testSurface: '3 test classes with overlapping review factory data',
      dependencies: '3 inbound callers across portal controllers',
    },
    whereItAppears: ['Guest Reviews', 'Business Travel Portal', 'VIP Guest Dashboard'],
    whyItMatters: [
      'The core review mapping (12 fields + 5 category ratings) is identical across all 3 methods.',
      'Adding a new rating category requires editing 3 separate files.',
    ],
    nextSteps: [
      'Extract a shared buildBaseReviewDTO method for the common 17 field mappings.',
      'Add guest-type-specific enrichment as composable post-mapping methods.',
      'Update callers to use base + enrichment pattern.',
    ],
    rationale: [
      '20 of 22 lines are identical across all 3 methods.',
      'Guest-type-specific fields are additive — a base + enrichment pattern fits naturally.',
    ],
  },

  /* ── Group 12: BooleanEqualityEvaluator.runEvaluator (NEAR DUPLICATE, method, MEDIUM, simple_logic) ── */
  {
    id: 'cg-012',
    name: 'BooleanEqualityEvaluator.runEvaluator...',
    level: 'method',
    tier: 'MEDIUM',
    category: 'simple_logic',
    effort: 'LOW',
    copies: 8,
    charsSaved: 6780,
    score: 72,
    isExact: false,
    recipe: 'Extract shared method. Simple boolean evaluation pattern is repeated 8 times.',
    recipeDetail: 'Eight methods implement the same pattern: retrieve a field value from a record, compare it against an expected value, and return a boolean result with optional logging. The methods differ only in the field name being evaluated and the comparison type (equality vs contains vs starts-with).',
    criticalitySignals: [
      { icon: '🔄', label: 'Copy Proliferation', detail: '8 copies — highest copy count in the org. Each new feature adds another copy of this pattern' },
      { icon: '🧪', label: 'Duplicate Tests', detail: '8 nearly identical test methods — 320 lines of duplicate test code' },
    ],
    representative: {
      id: 'cg-012-rep',
      fileName: 'BooleanEqualityEvaluator.cls',
      className: 'BooleanEqualityEvaluator',
      methodName: 'runEvaluator',
      lineRange: '8-22',
      lineCount: 15,
      charCount: 580,
      code: [
        'public static Boolean runEvaluator(SObject record, String fieldName, Object expectedValue) {',
        '    if (record == null || String.isBlank(fieldName)) {',
        '        return false;',
        '    }',
        '',
        '    Object actualValue = record.get(fieldName);',
        '    if (actualValue == null && expectedValue == null) {',
        '        return true;',
        '    }',
        '    if (actualValue == null || expectedValue == null) {',
        '        return false;',
        '    }',
        '',
        '    return String.valueOf(actualValue).equalsIgnoreCase(String.valueOf(expectedValue));',
        '}',
      ],
      referencedBy: ['RuleEngineService', 'WorkflowConditionEvaluator'],
      safeToRemove: false,
    },
    cloneCopies: [
      {
        id: 'cg-012-c1',
        fileName: 'StringEqualityEvaluator.cls',
        className: 'StringEqualityEvaluator',
        methodName: 'evaluate',
        lineRange: '5-20',
        lineCount: 16,
        charCount: 610,
        code: [
          'public static Boolean evaluate(SObject record, String fieldName, String expectedValue) {',
          '    if (record == null || String.isBlank(fieldName)) {',
          '        return false;',
          '    }',
          '',
          '    Object actualValue = record.get(fieldName);',
          '    if (actualValue == null && expectedValue == null) {',
          '        return true;',
          '    }',
          '    if (actualValue == null || expectedValue == null) {',
          '        return false;',
          '    }',
          '',
          '    String actual = String.valueOf(actualValue).trim();',
          '    return actual.equalsIgnoreCase(expectedValue.trim());',
          '}',
        ],
        referencedBy: ['ValidationRuleEngine'],
        safeToRemove: false,
        safetyNote: 'Trims whitespace before comparison; callers may depend on trim behavior.',
        differences: [
          {
            lineRange: '1',
            representativeCode: 'public static Boolean runEvaluator(SObject record, String fieldName, Object expectedValue) {',
            copyCode: 'public static Boolean evaluate(SObject record, String fieldName, String expectedValue) {',
            description: 'Method name differs; expectedValue typed as String instead of Object',
          },
          {
            lineRange: '14-15',
            representativeCode: '    return String.valueOf(actualValue).equalsIgnoreCase(String.valueOf(expectedValue));',
            copyCode: '    String actual = String.valueOf(actualValue).trim();\n    return actual.equalsIgnoreCase(expectedValue.trim());',
            description: 'Copy adds .trim() to both values before comparison',
          },
        ],
      },
      {
        id: 'cg-012-c2',
        fileName: 'ContainsEvaluator.cls',
        className: 'ContainsEvaluator',
        methodName: 'evaluate',
        lineRange: '5-20',
        lineCount: 16,
        charCount: 620,
        code: [
          'public static Boolean evaluate(SObject record, String fieldName, String searchValue) {',
          '    if (record == null || String.isBlank(fieldName)) {',
          '        return false;',
          '    }',
          '',
          '    Object actualValue = record.get(fieldName);',
          '    if (actualValue == null && searchValue == null) {',
          '        return true;',
          '    }',
          '    if (actualValue == null || searchValue == null) {',
          '        return false;',
          '    }',
          '',
          '    return String.valueOf(actualValue).toLowerCase().contains(searchValue.toLowerCase());',
          '}',
        ],
        referencedBy: ['SearchConditionEvaluator'],
        safeToRemove: false,
        safetyNote: 'Uses contains() instead of equals(); consolidation must preserve different comparison semantics.',
        differences: [
          {
            lineRange: '1',
            representativeCode: 'public static Boolean runEvaluator(SObject record, String fieldName, Object expectedValue) {',
            copyCode: 'public static Boolean evaluate(SObject record, String fieldName, String searchValue) {',
            description: 'Method name and parameter name differ',
          },
          {
            lineRange: '14',
            representativeCode: '    return String.valueOf(actualValue).equalsIgnoreCase(String.valueOf(expectedValue));',
            copyCode: '    return String.valueOf(actualValue).toLowerCase().contains(searchValue.toLowerCase());',
            description: 'Uses contains() instead of equalsIgnoreCase()',
          },
        ],
      },
      {
        id: 'cg-012-c3',
        fileName: 'StartsWithEvaluator.cls',
        className: 'StartsWithEvaluator',
        methodName: 'evaluate',
        lineRange: '5-19',
        lineCount: 15,
        charCount: 590,
        code: [
          'public static Boolean evaluate(SObject record, String fieldName, String prefix) {',
          '    if (record == null || String.isBlank(fieldName)) {',
          '        return false;',
          '    }',
          '',
          '    Object actualValue = record.get(fieldName);',
          '    if (actualValue == null && prefix == null) {',
          '        return true;',
          '    }',
          '    if (actualValue == null || prefix == null) {',
          '        return false;',
          '    }',
          '',
          '    return String.valueOf(actualValue).startsWithIgnoreCase(prefix);',
          '}',
        ],
        referencedBy: ['PrefixMatchEvaluator'],
        safeToRemove: false,
        safetyNote: 'Uses startsWith(); different comparison semantics.',
        differences: [
          {
            lineRange: '14',
            representativeCode: '    return String.valueOf(actualValue).equalsIgnoreCase(String.valueOf(expectedValue));',
            copyCode: '    return String.valueOf(actualValue).startsWithIgnoreCase(prefix);',
            description: 'Uses startsWithIgnoreCase() instead of equalsIgnoreCase()',
          },
        ],
      },
      {
        id: 'cg-012-c4',
        fileName: 'NumericEqualityEvaluator.cls',
        className: 'NumericEqualityEvaluator',
        methodName: 'runEvaluator',
        lineRange: '5-20',
        lineCount: 16,
        charCount: 640,
        code: [
          'public static Boolean runEvaluator(SObject record, String fieldName, Decimal expectedValue) {',
          '    if (record == null || String.isBlank(fieldName)) {',
          '        return false;',
          '    }',
          '',
          '    Object actualValue = record.get(fieldName);',
          '    if (actualValue == null && expectedValue == null) {',
          '        return true;',
          '    }',
          '    if (actualValue == null || expectedValue == null) {',
          '        return false;',
          '    }',
          '',
          '    Decimal actual = (Decimal) actualValue;',
          '    return actual == expectedValue;',
          '}',
        ],
        referencedBy: ['NumericRuleEvaluator'],
        safeToRemove: false,
        safetyNote: 'Uses Decimal comparison instead of String; must preserve numeric semantics.',
        differences: [
          {
            lineRange: '1',
            representativeCode: 'public static Boolean runEvaluator(SObject record, String fieldName, Object expectedValue) {',
            copyCode: 'public static Boolean runEvaluator(SObject record, String fieldName, Decimal expectedValue) {',
            description: 'expectedValue typed as Decimal instead of Object',
          },
          {
            lineRange: '14-15',
            representativeCode: '    return String.valueOf(actualValue).equalsIgnoreCase(String.valueOf(expectedValue));',
            copyCode: '    Decimal actual = (Decimal) actualValue;\n    return actual == expectedValue;',
            description: 'Uses direct Decimal comparison instead of String conversion',
          },
        ],
      },
      {
        id: 'cg-012-c5',
        fileName: 'DateEqualityEvaluator.cls',
        className: 'DateEqualityEvaluator',
        methodName: 'runEvaluator',
        lineRange: '5-20',
        lineCount: 16,
        charCount: 630,
        code: [
          'public static Boolean runEvaluator(SObject record, String fieldName, Date expectedValue) {',
          '    if (record == null || String.isBlank(fieldName)) {',
          '        return false;',
          '    }',
          '',
          '    Object actualValue = record.get(fieldName);',
          '    if (actualValue == null && expectedValue == null) {',
          '        return true;',
          '    }',
          '    if (actualValue == null || expectedValue == null) {',
          '        return false;',
          '    }',
          '',
          '    Date actual = (Date) actualValue;',
          '    return actual == expectedValue;',
          '}',
        ],
        referencedBy: ['DateRuleEvaluator'],
        safeToRemove: false,
        safetyNote: 'Uses Date cast and comparison; must preserve Date semantics.',
        differences: [
          {
            lineRange: '1',
            representativeCode: 'public static Boolean runEvaluator(SObject record, String fieldName, Object expectedValue) {',
            copyCode: 'public static Boolean runEvaluator(SObject record, String fieldName, Date expectedValue) {',
            description: 'expectedValue typed as Date instead of Object',
          },
          {
            lineRange: '14-15',
            representativeCode: '    return String.valueOf(actualValue).equalsIgnoreCase(String.valueOf(expectedValue));',
            copyCode: '    Date actual = (Date) actualValue;\n    return actual == expectedValue;',
            description: 'Uses direct Date comparison instead of String conversion',
          },
        ],
      },
      {
        id: 'cg-012-c6',
        fileName: 'BooleanFieldEvaluator.cls',
        className: 'BooleanFieldEvaluator',
        methodName: 'evaluate',
        lineRange: '5-18',
        lineCount: 14,
        charCount: 540,
        code: [
          'public static Boolean evaluate(SObject record, String fieldName, Boolean expectedValue) {',
          '    if (record == null || String.isBlank(fieldName)) {',
          '        return false;',
          '    }',
          '',
          '    Object actualValue = record.get(fieldName);',
          '    if (actualValue == null) {',
          '        return expectedValue == null || expectedValue == false;',
          '    }',
          '',
          '    Boolean actual = (Boolean) actualValue;',
          '    return actual == expectedValue;',
          '}',
        ],
        referencedBy: ['CheckboxRuleEvaluator'],
        safeToRemove: false,
        safetyNote: 'Treats null as false for Boolean fields; different null semantics than representative.',
        differences: [
          {
            lineRange: '7-9',
            representativeCode: '    if (actualValue == null && expectedValue == null) {\n        return true;\n    }',
            copyCode: '    if (actualValue == null) {\n        return expectedValue == null || expectedValue == false;\n    }',
            description: 'Treats null Boolean field as equivalent to false',
          },
        ],
      },
      {
        id: 'cg-012-c7',
        fileName: 'PicklistEvaluator.cls',
        className: 'PicklistEvaluator',
        methodName: 'evaluate',
        lineRange: '5-20',
        lineCount: 16,
        charCount: 590,
        code: [
          'public static Boolean evaluate(SObject record, String fieldName, String expectedValue) {',
          '    if (record == null || String.isBlank(fieldName)) {',
          '        return false;',
          '    }',
          '',
          '    Object actualValue = record.get(fieldName);',
          '    if (actualValue == null && expectedValue == null) {',
          '        return true;',
          '    }',
          '    if (actualValue == null || expectedValue == null) {',
          '        return false;',
          '    }',
          '',
          '    return String.valueOf(actualValue) == expectedValue;',
          '}',
        ],
        referencedBy: ['PicklistConditionEvaluator'],
        safeToRemove: false,
        safetyNote: 'Uses case-sensitive comparison (==) instead of equalsIgnoreCase; callers may depend on this.',
        differences: [
          {
            lineRange: '14',
            representativeCode: '    return String.valueOf(actualValue).equalsIgnoreCase(String.valueOf(expectedValue));',
            copyCode: '    return String.valueOf(actualValue) == expectedValue;',
            description: 'Uses case-sensitive == instead of equalsIgnoreCase()',
          },
        ],
      },
    ],
    impact: {
      codeLines: '124 total lines across 8 methods; ~96 lines of shared boilerplate',
      workflows: 'Rule engine evaluation across workflow conditions, validations, and search',
      changeRisk: 'Low — each evaluator is small and self-contained',
      testSurface: '8 test classes with nearly identical structure',
      dependencies: '8 inbound callers from various rule evaluator services',
    },
    whereItAppears: ['Workflow Rule Engine', 'Validation Rules', 'Search Conditions', 'Field Matching'],
    whyItMatters: [
      'Eight nearly identical evaluators bloat the codebase with ~96 lines of duplicated null-checking and field-access boilerplate.',
      'The null-handling strategy is inconsistent across evaluators (e.g., Boolean treats null as false).',
    ],
    nextSteps: [
      'Create a generic FieldEvaluator with a ComparisonStrategy enum (EQUALS, CONTAINS, STARTS_WITH).',
      'Add type-safe overloads for Decimal, Date, and Boolean that delegate to the generic evaluator.',
      'Migrate callers one evaluator at a time.',
    ],
    rationale: [
      'The null-check and field-access boilerplate (lines 2-12) is identical across all 8 evaluators.',
      'Only the comparison operation on the final line differs — a strategy pattern fits perfectly.',
    ],
  },
];

/* ─── Score History ─── */

export const scoreHistory = [
  { month: 'Jan', score: 62 },
  { month: 'Feb', score: 65 },
  { month: 'Mar 12', score: 69 },
  { month: 'Mar 26', score: 78 },
];

/* ─── Level Distribution ─── */

export const levelDistribution = [
  { name: 'File-level', value: 142, color: '#ea001e' },
  { name: 'Method-level', value: 126, color: '#fe9339' },
  { name: 'Block-level', value: 50, color: '#0176d3' },
];

/* ─── Category Distribution ─── */

export const categoryDistribution = [
  { name: 'file_duplicate', value: 142 },
  { name: 'business_logic', value: 68 },
  { name: 'data_access_logic', value: 45 },
  { name: 'orchestration', value: 28 },
  { name: 'simple_logic', value: 22 },
  { name: 'trivial_accessor', value: 13 },
];

/* ─── Apex Guru Insights ─── */

export const apexGuruInsights = [
  {
    id: 'ag-001',
    title: 'CPU hotspot in OpportunityTriggerHandler',
    severity: 'Critical',
    impact: 'High',
    team: 'Sales Engineering',
    description: 'The beforeUpdate method in OpportunityTriggerHandler contains nested loops over territory assignments that consume excessive CPU time on bulk updates (200+ records). Refactoring to a map-based lookup could reduce CPU time by ~60%.',
    lastDetected: '2026-03-25',
  },
  {
    id: 'ag-002',
    title: 'Heap allocation spike in AccountMergeService',
    severity: 'Warning',
    impact: 'Medium',
    team: 'Data Quality',
    description: 'AccountMergeService.mergeAccounts() loads all related contacts and opportunities into memory before processing. For accounts with 1,000+ children, this approaches the 6MB synchronous heap limit. Implementing chunked processing would mitigate the risk.',
    lastDetected: '2026-03-24',
  },
  {
    id: 'ag-003',
    title: 'SOQL in loop in QuotePricingHelper',
    severity: 'Critical',
    impact: 'High',
    team: 'Sales Operations',
    description: 'QuotePricingHelper.applyVolumeDiscount() issues a SOQL query inside a for-loop when processing multi-line quotes. This is a known governor limit risk that has caused failures on quotes with 80+ line items.',
    lastDetected: '2026-03-23',
  },
  {
    id: 'ag-004',
    title: 'DML optimization in CaseEscalationBatch',
    severity: 'Info',
    impact: 'Low',
    team: 'Service Cloud Team',
    description: 'CaseEscalationBatch performs individual DML updates inside the execute method loop. Collecting records and performing a single bulk DML at the end of each batch chunk would improve throughput and reduce transaction overhead.',
    lastDetected: '2026-03-22',
  },
];

/* ─── Modernisation Candidates ─── */

export const modernisationCandidates = [
  {
    id: 'mod-001',
    title: 'Migrate LegacyPricingUtils to PricingRulesEngineV2',
    category: 'Code Consolidation',
    impact: 'High',
    team: 'Revenue Cloud Platform',
    status: 'Recommended',
  },
  {
    id: 'mod-002',
    title: 'Replace inline SOQL in QuotePricingHelper with selector pattern',
    category: 'Governor Limit Risk',
    impact: 'High',
    team: 'Sales Operations',
    status: 'Recommended',
  },
  {
    id: 'mod-003',
    title: 'Consolidate ForecastRollupHelper + ForecastUtils into ForecastRollupService',
    category: 'Code Consolidation',
    impact: 'Medium',
    team: 'Revenue Operations',
    status: 'Planned',
  },
  {
    id: 'mod-004',
    title: 'Extract CaseEscalation trigger code into handler class',
    category: 'Architecture',
    impact: 'Medium',
    team: 'Service Cloud Team',
    status: 'Under Review',
  },
  {
    id: 'mod-005',
    title: 'Unify address validation behind AddressValidationService',
    category: 'Code Consolidation',
    impact: 'Medium',
    team: 'Commerce Engineering',
    status: 'Recommended',
  },
  {
    id: 'mod-006',
    title: 'Migrate hardcoded approval thresholds to Custom Metadata',
    category: 'Configuration',
    impact: 'High',
    team: 'Deal Desk',
    status: 'Planned',
  },
];

/* ─── Agentforce: Initial Messages ─── */

export const initialAgentMessages: AgentMessage[] = [
  {
    id: 'agent-welcome-001',
    role: 'bot',
    content:
      "Hi, I'm Agentforce! I can help you analyze code clones, identify consolidation opportunities, and guide refactoring. What can I help you with?",
    timestamp: new Date().toISOString(),
    actions: [
      { label: 'Run a Clone Detection scan', actionId: 'scan' },
      { label: 'Show me clone groups', actionId: 'navigate' },
      { label: "What's changed since my last report?", actionId: 'query' },
    ],
  },
];

/* ─── Agentforce: Conversation Flows ─── */

export const agentConversations: Record<string, AgentMessage[]> = {
  'scan-summary': [
    {
      id: 'scan-s-001',
      role: 'user',
      content: 'Run a Clone Detection scan',
      timestamp: '2026-03-26T07:00:00Z',
    },
    {
      id: 'scan-s-002',
      role: 'bot',
      content:
        "I've completed the scan of your Northstar Retail Group production org. Here's a summary:\n\n• **Reuse Score:** 78 / 100 (+9 from last scan)\n• **Clone Groups Found:** 12 high-priority groups across Apex and Triggers\n• **Characters Saveable:** ~83,527 characters of duplicated code\n• **Top Issue:** 7 identical GetCases DataView files (13,608 chars)\n\nThe biggest win is consolidating the 7 identical case query files into a single parameterized class.",
      timestamp: '2026-03-26T07:00:05Z',
      actions: [
        { label: 'Show high-priority groups', actionId: 'navigate' },
        { label: 'Compare with last report', actionId: 'query' },
      ],
    },
  ],
  'clone-analysis': [
    {
      id: 'cl-a-001',
      role: 'user',
      content: 'Analyze the GetCasesByAssigneeId clone group',
      timestamp: '2026-03-26T07:01:00Z',
    },
    {
      id: 'cl-a-002',
      role: 'bot',
      content:
        "Here's my analysis of the **GetCasesByAssigneeIdDataView** clone group:\n\n**What's happening:** Seven identical Apex classes each expose a REST endpoint to query Cases. They share the same DTO mapping, error handling, and query structure — the only difference is the WHERE clause filter field.\n\n**Why it matters:**\n• A bug fix to the DTO mapping must be applied 7 times\n• Each file has its own test class, multiplying maintenance by 7x\n• 13,608 characters of pure duplication\n\n**Recommendation:** Create a single parameterized `GetCasesByFilterDataView` class that accepts the filter field and value. Delete the other 6 files.\n\n**Effort:** LOW — straightforward mechanical refactor.",
      timestamp: '2026-03-26T07:01:08Z',
      actions: [
        { label: 'Show me the code', actionId: 'navigate' },
        { label: 'Create refactoring tasks', actionId: 'action' },
        { label: 'Which callers are affected?', actionId: 'query' },
      ],
    },
  ],
  'report-comparison': [
    {
      id: 'rpt-c-001',
      role: 'user',
      content: "What's changed since my last report?",
      timestamp: '2026-03-26T07:03:00Z',
    },
    {
      id: 'rpt-c-002',
      role: 'bot',
      content:
        "Comparing your **Mar 26** report (score: 78) to the **Mar 12** report (score: 69):\n\n**Score improved by +9 points.** Here's what changed:\n\n✅ **Resolved (2 groups):**\n• Lead Scoring duplicate classes were deleted — saved ~120 lines\n• ForecastUtils was consolidated into ForecastRollupService\n\n📈 **Improved (3 groups):**\n• BooleanEqualityEvaluator — 2 of 8 copies consolidated\n• ViaductListingDataService — test coverage increased from 68% to 91%\n• FSCEvidenceValidation — one variant migrated to strategy pattern\n\n⚠️ **New (1 group):**\n• A new near-duplicate detected in AvailabilitySearchService — appears to be from the Q2 booking feature sprint\n\n**Net:** 12 active clone groups, 5 high-priority.",
      timestamp: '2026-03-26T07:03:10Z',
      actions: [
        { label: 'Show resolved groups', actionId: 'navigate' },
        { label: 'Drill into the new group', actionId: 'navigate' },
        { label: 'Export comparison report', actionId: 'action' },
      ],
    },
  ],
};
